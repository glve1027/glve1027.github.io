<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://glve1027.github.io/</id>
    <title>Ghcoder</title>
    <updated>2020-01-28T15:59:47.338Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://glve1027.github.io/"/>
    <link rel="self" href="https://glve1027.github.io/atom.xml"/>
    <subtitle>一个不断在修炼的程序员</subtitle>
    <logo>https://glve1027.github.io/images/avatar.png</logo>
    <icon>https://glve1027.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Ghcoder</rights>
    <entry>
        <title type="html"><![CDATA[整理C++笔记]]></title>
        <id>https://glve1027.github.io/post/zheng-li-xue-xi-cbi-ji</id>
        <link href="https://glve1027.github.io/post/zheng-li-xue-xi-cbi-ji">
        </link>
        <updated>2020-01-28T15:54:39.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="学习c-的重要性不言而喻春节在家没事就整理了一下c的知识点">学习C++ 的重要性不言而喻，春节在家没事就整理了一下C++的知识点。</h2>
]]></summary>
        <content type="html"><![CDATA[<h2 id="学习c-的重要性不言而喻春节在家没事就整理了一下c的知识点">学习C++ 的重要性不言而喻，春节在家没事就整理了一下C++的知识点。</h2>
<!-- more -->
<h3 id="第一天">第一天</h3>
<ol>
<li>c++ 中是不能同时存在两个<code>main</code>函数。</li>
<li><code>getchar()</code> 等待键盘输入。</li>
<li>输出: <code>cout &lt;&lt; &quot;xx&quot;</code>。</li>
<li><code>endl</code>: 换行的意思。</li>
<li>键盘赋值给： <code>cin &gt;&gt; xx</code>。</li>
<li>C语言不支持函数重载。【重载 -&gt; 函数名相同，参数的个数，参数的类型，参数的顺序不同。】</li>
<li>extern &quot;C&quot;/ extern &quot;C&quot; { } -&gt; 按照C语言的方式编译。</li>
<li>extern &quot;C&quot;/ extern &quot;C&quot; { } 使用在函数声明的地方。</li>
<li>c++ 支持默认参数，【默认参数只能从右边开始】</li>
<li>c++ 函数中使用默认参数，既有<code>定义</code>，也有<code>实现</code>，只能将默认参数使用在<code>定义</code>中 【可是是<code>常量</code>，也可以是<code>全局变量</code>】</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[学习优秀的iOS代码（一）]]></title>
        <id>https://glve1027.github.io/post/xue-xi-you-xiu-de-ios-dai-ma-yi</id>
        <link href="https://glve1027.github.io/post/xue-xi-you-xiu-de-ios-dai-ma-yi">
        </link>
        <updated>2020-01-22T13:31:58.000Z</updated>
        <summary type="html"><![CDATA[<p><a href="https://github.com/SDWebImage/SDWebImage">SDWebImage</a>,一个iOS图片加载库，在iOS界，一直是标杆的存在，本篇博客不去讨论他的具体代码架构、设计、算法，而是从一个个小的tips来发现这些代码的闪光点。</p>
]]></summary>
        <content type="html"><![CDATA[<p><a href="https://github.com/SDWebImage/SDWebImage">SDWebImage</a>,一个iOS图片加载库，在iOS界，一直是标杆的存在，本篇博客不去讨论他的具体代码架构、设计、算法，而是从一个个小的tips来发现这些代码的闪光点。</p>
<!-- more -->
<h3 id="1-协议接口的使用">1. 协议(接口)的使用</h3>
<ul>
<li>自吹一下，作者的这一点写代码的风格和我还是很像的，都很喜欢<code>面向接口编程</code>，整个<code>SDWebImage</code>中存在很多这样的情况，比如：<code>SDImageCodersManager</code>， 光从这个名字就能看出这是一个关于给图片编码的类，再看定义：</li>
</ul>
<pre><code>@interface SDImageCodersManager : NSObject &lt;SDImageCoder&gt;
@end

@protocol SDImageCoder &lt;NSObject&gt;
@end
</code></pre>
<ul>
<li>这样代码风格遍布在整个项目中, 例如下面类中定义的<code>SDWebImageDownloaderRequestModifier</code>,<code>SDWebImageDownloaderResponseModifier</code>,<code>SDWebImageDownloaderDecryptor</code></li>
</ul>
<pre><code>@property (nonatomic, strong, nullable) id&lt;SDWebImageDownloaderRequestModifier&gt; requestModifier;

@property (nonatomic, strong, nullable) id&lt;SDWebImageDownloaderResponseModifier&gt; responseModifier;

@property (nonatomic, strong, nullable) id&lt;SDWebImageDownloaderDecryptor&gt; decryptor;
</code></pre>
<blockquote>
<p>总结：如果后面我们也想实现一个功能可以模仿这种规范：1：定义一个接口  2：定义一个默认的类并且遵循相应的接口  3：暴露给外面的是一个id&lt;协议&gt;object。</p>
</blockquote>
<h3 id="2-宏的使用">2. 宏的使用</h3>
<ul>
<li>其实如果你正常看第三个优秀源码，你就会发现，很多代码存在很多优秀的宏定义，一方面使用起来很优雅，一方面也可以学习到关于编译器的一个参数的设置。在项目中，我经常看到下面的代码：</li>
</ul>
<pre><code>SD_LOCK(self.HTTPHeadersLock);
[self.HTTPHeaders setValue:value forKey:field];
SD_UNLOCK(self.HTTPHeadersLock);

// 定义
#ifndef SD_LOCK
#define SD_LOCK(lock) dispatch_semaphore_wait(lock, DISPATCH_TIME_FOREVER);
#endif

#ifndef SD_UNLOCK
#define SD_UNLOCK(lock) dispatch_semaphore_signal(lock);
#endif

// 用来区分平台的宏
#if TARGET_OS_IOS || TARGET_OS_TV
    #define SD_UIKIT 1
#else
    #define SD_UIKIT 0
#endif

#if TARGET_OS_IOS
    #define SD_IOS 1
#else
    #define SD_IOS 0
#endif
</code></pre>
<blockquote>
<p>总结：没什么技术含量，大家都会这样去实现<code>锁</code>,但是代码未必有这么优雅，点赞。<br>
说道<code>锁</code>的实现，代码中还存在另一个保证线程安全的就是<code>@synchronized(self)</code>, 这里在项目源码中，有时候作者用的是<code>@synchronized(self)</code>, 有时候用的又是一个临时变量：<code>@synchronized(xx)</code>, 网上之前也有一些关于这样<code>性能不是很好的传闻</code>。关于这个我特意去查阅了一些资料，大家可以看一下<a href="http://mrpeak.cn/blog/synchronized/">FaceBook大神的这篇文章</a></p>
</blockquote>
<h3 id="3-关于函数定义规范的问题">3. 关于函数定义规范的问题</h3>
<ul>
<li>在项目中关于下载图片的<code>api</code>，可以说是核心的代码，在看看内部函数定义，代码如下：</li>
</ul>
<pre><code>// 参数类型的定义
typedef void(^SDWebImageNoParamsBlock)(void);
typedef NSString * SDWebImageContextOption NS_EXTENSIBLE_STRING_ENUM;
typedef NSDictionary&lt;SDWebImageContextOption, id&gt; SDWebImageContext;
typedef NSMutableDictionary&lt;SDWebImageContextOption, id&gt; SDWebImageMutableContext;


- (void)sd_setImageWithURL:(nullable NSURL *)url {
    [self sd_setImageWithURL:url placeholderImage:nil options:0 progress:nil completed:nil];
}

- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder {
    [self sd_setImageWithURL:url placeholderImage:placeholder options:0 progress:nil completed:nil];
}

- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options {
    [self sd_setImageWithURL:url placeholderImage:placeholder options:options progress:nil completed:nil];
}

- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options context:(nullable SDWebImageContext *)context
</code></pre>
<ul>
<li>后期定义函数也可以这样，最后收拢到一个核心的函数上。</li>
<li>关于参数的类型，是否需要提前定义，这个从规整的角度来看，还是需要的。</li>
<li>关于函数类型是否为<code>nil</code>，还是需要<code>nullable</code>来标识。</li>
</ul>
<blockquote>
<p>越是规范的东西，越是在写代码的时候可以的去实现，时间长了，也可以学习到优秀代码的规范。</p>
</blockquote>
<h3 id="4-关于网络请求的实现">4. 关于网络请求的实现</h3>
<ul>
<li>这里用自定义的<code>NSOperation</code>, 这里要说一下，自定义<code>NSOperation</code>有两种实现，一种是<code>从写start方法</code>【需要自己去管理isFinish/isExecuting】, 领一种是<code>从写main方法</code>【不需要管理状态值】</li>
<li>并且这里上来就用了<code>@synchronized</code>保证线程安全。</li>
<li>并且这里使用了一个自定义的串行队列来管理<code>_coderQueue = dispatch_queue_create(&quot;com.hackemist.SDWebImageDownloaderOperationCoderQueue&quot;, DISPATCH_QUEUE_SERIAL);</code></li>
</ul>
<pre><code>- (void)start {
    @synchronized (self) {
        ...
    }
}
</code></pre>
<h3 id="5-关于代码设计开闭原则">5. 关于代码设计：<code>开闭原则</code></h3>
<ul>
<li>其实在设计<code>Framework</code>的时候，需要时刻记着，哪些东西不应该暴露给用户，哪些需要暴露给用户修改的，这里作者大量使用枚举来实现外部自定义配置：</li>
</ul>
<pre><code>if (self.options &amp; SDWebImageDownloaderHighPriority) {
    self.dataTask.priority = NSURLSessionTaskPriorityHigh;
} else if (self.options &amp; SDWebImageDownloaderLowPriority) {
    self.dataTask.priority = NSURLSessionTaskPriorityLow;
}      
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[DFS&BFS 解题心得]]></title>
        <id>https://glve1027.github.io/post/dfsandbfs-jie-ti-xin-de</id>
        <link href="https://glve1027.github.io/post/dfsandbfs-jie-ti-xin-de">
        </link>
        <updated>2019-12-26T02:12:29.000Z</updated>
        <summary type="html"><![CDATA[<blockquote>
<p>最近在一直在刷LeetCode, 一直在练习DFS与BFS的题，练多了发现有很多都是有固定套路的心得：</p>
</blockquote>
]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>最近在一直在刷LeetCode, 一直在练习DFS与BFS的题，练多了发现有很多都是有固定套路的心得：</p>
</blockquote>
  <!-- more -->
<ul>
<li>先写一段伪代码，这段伪代码是下面套路的“白话文”</li>
</ul>
<pre><code>// 1. 这里的value 是初始值
func start(value) {
    // 2. 初始值的边界问题
    if (value 不合法) { return }

    // 3. !!关键，这里一定要新建一个函数，用于递归, 见下面的函数bfs
    // 同时，bfs函数的入参，你可以自己不断的加
    // 参数的理解， 如果bfs函数的终止条件 需要通过value 来计算出来，这里就有必要传入进去
    let terminalValue = func handle(value)
    // 参数1：
    bfs(terminalValue, [value])
    // 或者dfs
    dfs(terminalValue, value, [])
}

private func bfs(terminalValue, [value]) {
    // 4. 判断终止条件
    if (terminalValue) { return }
    // 5. 这里需要一个临时的集合，用数组Array
    var tempArray = []

    for v in [value] {
        // 6. !!这里通过v来找到下一个需要遍历的量，这里既可以实现DFS, 也可以实现BFS
        let nextV = func findNextV(v)
        
        tempArray.append(nextV)
    }

    // 递归调用
    bfs(terminalValue, tempArray)
}


private func dfs(terminalValue, value, arr) {
    // 4. 判断终止条件
    if (terminalValue) { return }
    
    // 5. 这里是关键！！判断arr数组逻辑关系
    
    var arrLeftDatas = arr
    if let leftNode = value.left {
        arrLeftDatas.append(leftNode)
        
        dfs(terminalValue, value, arrLeftDatas)
    }
    
    var arrRightDatas = arr
    if let rightNode = value.right {
        arrRightDatas.append(rightNode)
        
        dfs(terminalValue, value, arrRightDatas)
    }
}
</code></pre>
<ul>
<li>
<p>下面看两道题目：</p>
</li>
<li>
<p>LeetCode 199题目</p>
</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://glve1027.github.io//post-images/1577333311197.jpg" alt="" loading="lazy"></figure>
<pre><code>import Foundation

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     public var val: Int
 *     public var left: TreeNode?
 *     public var right: TreeNode?
 *     public init(_ val: Int) {
 *         self.val = val
 *         self.left = nil
 *         self.right = nil
 *     }
 * }
 */
class Solution {
    func rightSideView(_ root: TreeNode?) -&gt; [Int] {
        guard let root = root else { return [] }

        var datas: [TreeNode] = [root]
        var result: [Int] = [root.val]
        var currentLevel: Int = 1

        printRight(&amp;datas, &amp;result, &amp;currentLevel)
        return result
    }

    private func printRight(_ datas: inout [TreeNode], _ result: inout [Int], _ level: inout Int) {
        guard !datas.isEmpty else { return }

        // 记录层数
        level += 1

        var tempDatas: [TreeNode] = []
        for d in datas {
            // 下一个循环要检查的子数，这里都要记录下来
            if let r = d.right {
                tempDatas.append(r)
                if result.count &lt; level { result.append(r.val) }
            }
            if let l = d.left {
                tempDatas.append(l)
                if result.count &lt; level { result.append(l.val) }
            }
        }

        printRight(&amp;tempDatas, &amp;result, &amp;level)
    }
}
</code></pre>
<ul>
<li>LeetCode 994</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://glve1027.github.io//post-images/1577333441345.jpg" alt="" loading="lazy"></figure>
<pre><code>
import Foundation

class Solution {
    func orangesRotting(_ grid: [[Int]]) -&gt; Int {
        guard !grid.isEmpty else { return 0 }

        var bads: [[Int]] = []
        var visited: [[Int]] = []
        var grid = grid
        var total: Int = 0
        var maxY: Int = 0
        let maxX: Int = grid.count - 1

        for i in 0..&lt;grid.count {
            let temp = grid[i]
            maxY = max(maxY, temp.count - 1)
            for j in 0..&lt;temp.count {
                if (temp[j] == 2) {
                    bads.append([i, j])
                    visited.append([i, j])
                }
                total += 1
            }
        }

        // 起始条件判断
        if (bads.isEmpty) { return hasNoBad(grid) ? -1 : 0  }

        var minu: Int = 0
        countMinute(&amp;minu, maxX, maxY, &amp;grid, &amp;bads, &amp;visited, total)
        return hasNoBad(grid) ?  -1 : minu
    }

    private func countMinute(_ minu: inout Int, _ maxX: Int, _ maxY: Int, _ grid: inout [[Int]], _ bads: inout [[Int]], _ visited: inout [[Int]], _ total: Int) {
        guard !bads.isEmpty else { return }
        guard visited.count &lt; total else { return }

        // 临时收集坏的
        var visited = visited
        var tempBads: [[Int]] = []
        // 遍历
        for b in bads {
            let currentX = b[0]
            let currentY = b[1]
            let currentIsBad = grid[currentX][currentY] == 2
            // 四个方向进行判断
            
            // 1. 上
            let upY = currentY - 1
            if (upY &gt;= 0 &amp;&amp; upY &lt;= maxY &amp;&amp; !containsInDatas(visited, [currentX, upY])) {
                visited.append([currentX, upY])
                if (currentIsBad &amp;&amp; grid[currentX][upY] == 1) {
                    // 已经腐烂
                    grid[currentX][upY] = 2
                    tempBads.append([currentX, upY])
                }
            }

            // 2. 左
            let leftX = currentX - 1
            if (leftX &gt;= 0 &amp;&amp; leftX &lt;= maxX &amp;&amp; !containsInDatas(visited, [leftX, currentY])) {
                visited.append([leftX, currentY])
                if (currentIsBad &amp;&amp; grid[leftX][currentY] == 1) {
                    // 已经腐烂
                    grid[leftX][currentY] = 2
                    tempBads.append([leftX, currentY])
                }
            }

            // 3. 下
            let downY = currentY + 1
            if (downY &gt;= 0 &amp;&amp; downY &lt;= maxY &amp;&amp; !containsInDatas(visited, [currentX, downY])) {
                visited.append([currentX, downY])
                if (currentIsBad &amp;&amp; grid[currentX][downY] == 1) {
                    // 已经腐烂
                    grid[currentX][downY] = 2
                    tempBads.append([currentX, downY])
                }
            }

            // 4. 右
            let rightX = currentX + 1
            if (rightX &gt;= 0 &amp;&amp; rightX &lt;= maxX &amp;&amp; !containsInDatas(visited, [rightX, currentY])) {
                visited.append([rightX, currentY])
                if (currentIsBad &amp;&amp; grid[rightX][currentY] == 1) {
                    // 已经腐烂
                    grid[rightX][currentY] = 2
                    tempBads.append([rightX, currentY])
                }
            }
        }

        // 如果已经没有坏橘子，就提前退出
        guard noBads(tempBads, grid) == false else { return }
        // 累计时间
        minu += 1
        countMinute(&amp;minu, maxX, maxY, &amp;grid, &amp;tempBads, &amp;visited, total)
    }

    private func hasNoBad(_ grid: [[Int]]) -&gt; Bool {
        guard !grid.isEmpty else { return false }
        for i in 0..&lt;grid.count {
            let temp = grid[i]
            for j in 0..&lt;temp.count {
                if (temp[j] == 1) { return true }
            }
        }
        return false
    }

    private func noBads(_ bads: [[Int]], _ grid: [[Int]]) -&gt; Bool {
        guard !bads.isEmpty else { return true }
        for b in bads {
            let x = b[0]
            let y = b[1]
            if (grid[x][y] == 2) { return false }
        }
        return true
    }

    private func containsInDatas(_ datas: [[Int]], _ e: [Int]) -&gt; Bool {
        guard !datas.isEmpty else { return false }
        for d in datas where e == d {
            return true
        }
        return false
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[iOS多线程操作]]></title>
        <id>https://glve1027.github.io/post/ios-duo-xian-cheng-cao-zuo</id>
        <link href="https://glve1027.github.io/post/ios-duo-xian-cheng-cao-zuo">
        </link>
        <updated>2019-12-18T07:39:14.000Z</updated>
        <summary type="html"><![CDATA[<p>一旦项目中涉及到多线程操作，就会显得异常复杂，尤其是对数据的操作，如何保证高效的“多读单写”呢？</p>
]]></summary>
        <content type="html"><![CDATA[<p>一旦项目中涉及到多线程操作，就会显得异常复杂，尤其是对数据的操作，如何保证高效的“多读单写”呢？</p>
<!-- more -->
<p>方案有很多种：</p>
<ul>
<li>可以使用<code>锁</code>[这里就不具体展开了，锁的实现太多了]，这里重点说一下第二个方案：</li>
<li>利用GCD实现“多读单写”的功能</li>
</ul>
<pre><code>// 创建一个全局的自定义并行队列
dispatch_queue_t globalQueue = dispatch_queue_create(&quot;xxx&quot;, DISPATCH_QUEUE_CONCURRENT);
// 对于&quot;写&quot;的操作
// 1. 保证在我写之前所有的操作已经完成。2. 也只有在我完成此次&quot;写&quot;的操作的时候，下面的操作才能继续执行。
dispatch_barrier_async(globalQueue, ^{ });
// 对于&quot;读&quot;的操作, 同步的获取值
dispatch_sync(globalQueue, ^{ });
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[App主题换肤功能]]></title>
        <id>https://glve1027.github.io/post/app-zhu-ti-huan-fu-gong-neng</id>
        <link href="https://glve1027.github.io/post/app-zhu-ti-huan-fu-gong-neng">
        </link>
        <updated>2019-12-17T16:19:47.000Z</updated>
        <summary type="html"><![CDATA[<p>现在国内很多App，每到节假日，都会搞一些活动，这样就免不了需要一个统一管理所谓“皮肤”功能【这里主要指图片、字体、颜色等资源】，下面就是我设计的”皮肤“功能模块。</p>
]]></summary>
        <content type="html"><![CDATA[<p>现在国内很多App，每到节假日，都会搞一些活动，这样就免不了需要一个统一管理所谓“皮肤”功能【这里主要指图片、字体、颜色等资源】，下面就是我设计的”皮肤“功能模块。</p>
<!-- more -->
<h2 id="主题皮肤切换模块">主题皮肤切换模块</h2>
<h3 id="gcttheme配置篇">GCTTheme配置篇</h3>
<ul>
<li>调用<code>GCTThemeManager</code>之前需要注册配置数据, 调用的时机目前是在<code>didFinishLaunchingWithOptions</code>的时候 （这个后期可以修改！！）</li>
</ul>
<pre><code>// 1. GCTThemeSwitchObject 控制开关的类，灵活控制开关就可以配置这个类 （面向协议）
GCTThemeSwitchObject *switchObj = [GCTThemeSwitchObject new];
[switchObj setGCTThemeSwitch:YES];
// 2. GCTThemeData 数据源的类，目前只读取本地数据，后期远程数据只需要配置它（面向协议）
[[GCTThemeManager sharedInstanceWithThemeData:[GCTThemeData new]] switchBaseOn:switchObj];
</code></pre>
<h3 id="gcttheme使用篇">GCTTheme使用篇</h3>
<ul>
<li>方法1：如果要修改的控件是<strong>UIView或者是它的子类</strong>并且这个<strong>UIView不等于nil</strong>，可以直接调用方法</li>
</ul>
<pre><code>- (void)configSkinWithModule:(GCTThemeModuleTypes)module skinDatas:(NSDictionary&lt;NSString *, NSString *&gt; *)skinDatas;
</code></pre>
<ul>
<li>方法2：如果上面的条件有其中一个不满足，可以直接调用单例<code>GCTThemeManager</code></li>
</ul>
<pre><code>// 需要配置皮肤替换功能的，图片获取需要 1. moduleName  2. imgKey
- (UIImage *)getImageByKey:(NSString *)imgKey from:(GCTThemeModuleTypes)module;
- (NSString *)getImageNameByKey:(NSString *)imgKey from:(GCTThemeModuleTypes)module;
// 需要配置皮肤替换功能的，颜色获取需要 1. moduleName  2. colorKey
- (UIColor *)getColorByKey:(NSString *)colorKey from:(GCTThemeModuleTypes)module;
// 需要配置皮肤替换功能的，文字获取需要 1. moduleName  2. titleKey
- (NSString *)getTitleByKey:(NSString *)titleKey from:(GCTThemeModuleTypes)module;
</code></pre>
<h4 id="实际使用情况">实际使用情况</h4>
<ul>
<li>直接配置TabBarController图片、文字。【<strong>因为tabBarItem默认是懒加载的，所以只能通过GCTThemeManager去实现</strong>】</li>
</ul>
<pre><code>navController.tabBarItem.title = [[GCTThemeManager sharedInstance] getTitleByKey:model.titleKey from:GCTThemeModuleTypeHomeTabbar];
navController.tabBarItem.image = [UIImage imageMakeWithName:model.imageName type:@&quot;png&quot;];
NSString *imageName = [[GCTThemeManager sharedInstance] getImageNameByKey:model.imageSelectedKey from:GCTThemeModuleTypeHomeTabbar];
navController.tabBarItem.selectedImage = [[UIImage imageMakeWithName:imageName type:@&quot;png&quot;] imageWithRenderingMode:UIImageRenderingModeAlwaysOriginal];
</code></pre>
<ol>
<li><strong>在默认情况下，使用的是背景色</strong>。</li>
<li><strong>在春节皮肤的配置下使用的是图片</strong> 【这里可以以此类推，某种皮肤下面使用的属性1，在另一种皮肤下使用的是属性2，因为当找不到对应的属性值的时候返回nil，也就是不实现】</li>
</ol>
<pre><code>self.tipsView.homeBG.image = [[GCTThemeManager sharedInstance] getImageByKey:@&quot;home_wifi_scaning_unconnected&quot; from:GCTThemeModuleTypeHomeWifi];
self.backgroundColor = [[GCTThemeManager sharedInstance] getColorByKey:@&quot;home_wifi_found_scaning&quot; from:GCTThemeModuleTypeHomeWifi];
</code></pre>
<h3 id="gcttheme原理篇">GCTTheme原理篇</h3>
<ul>
<li>GCTThemeSwitchObject遵循的协议</li>
</ul>
<pre><code>@protocol GCTThemeSwitchProtocol &lt;NSObject&gt;

- (void)setGCTThemeSwitch:(BOOL)isOpen;
- (BOOL)GCTThemeSwitchIsOpen;

@end
</code></pre>
<ul>
<li>数据源GCTThemeData遵循的协议</li>
</ul>
<pre><code>typedef enum : NSUInteger {
    GCTThemeDefaultData,
    GCTThemeFestivalData
} GCTThemeDataTypes;

NS_ASSUME_NONNULL_BEGIN

@protocol GCTThemeDataProtocol &lt;NSObject&gt;

// Getter Method
- (NSDictionary *)skinDataFor:(GCTThemeDataTypes)themeType;

// Setter Method
- (void)setSkinData:(NSDictionary *)data for:(GCTThemeDataTypes)themeType;

@end
</code></pre>
<ul>
<li><strong>GCTThemeManager核心代码</strong></li>
</ul>
<pre><code>- (NSString *)findResourceBy:(NSString *)key from:(GCTThemeModuleTypes)module with:(NSString *)subKey {
    // 切换数据
    NSDictionary *resourcesData = self.switchIsOpen ? self.tempSkinData : self.skinData;
    NSString *moduleString = [self convertThemeType2String:module];
    if (moduleString == nil || [moduleString isEqualToString:@&quot;&quot;] || resourcesData == nil || ![resourcesData.allKeys containsObject:moduleString]) return nil;
    NSDictionary *moduleDic = resourcesData[moduleString];
    if (![moduleDic.allKeys containsObject:subKey]) { return nil; }
    NSDictionary *Dic = moduleDic[subKey];
    if (![Dic.allKeys containsObject:key]) { return nil; }
    return Dic[key];
}
</code></pre>
<h3 id="测试篇">测试篇</h3>
<ul>
<li>配置以及Mock数据</li>
</ul>
<pre><code>- (void)setUp {
    self.themeData = [GCTThemeData new];
    // Mock Data
    [self.themeData setSkinData:@{@&quot;home_tabbar&quot;: @{@&quot;images&quot;: @{@&quot;image_home_button_selected&quot;: @&quot;Home_Selected&quot;,
                                                                 @&quot;image_video_button_selected&quot;: @&quot;Provider_Selected&quot;,
                                                                 @&quot;image_discovery_button_selected&quot;: @&quot;Discovery_Selected&quot;,
                                                                 @&quot;image_mine_button_selected&quot;: @&quot;Mine_Selected&quot;}}} for:GCTThemeDefaultData];
    [self.themeData setSkinData:@{@&quot;home_middle&quot;: @{@&quot;colors&quot;: @{@&quot;color_before_background&quot;: @&quot;#cccccc&quot;},
                                                    @&quot;values&quot;: @{@&quot;value_home_title&quot;: @&quot;测试&quot;}
                                                    }} for:GCTThemeFestivalData];
    
    
    // Mock Switch
    self.switchObject = [GCTThemeSwitchObject new];
    [self.switchObject setGCTThemeSwitch:NO];
    
    self.manager = [GCTThemeManager sharedInstanceWithThemeData:self.themeData];
    [self.manager switchBaseOn:self.switchObject];
}

/*
 ** 测试是否为单例
 */
- (void)testGCTThemeManagerIsSingle {
    NSMutableArray *managers = [NSMutableArray array];
    
    for (int i = 0; i &lt; 3; i++) {
        dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
            GCTThemeManager *tempManager = [[GCTThemeManager alloc] init];
            [managers addObject:tempManager];
        });
    }
   
    [managers enumerateObjectsUsingBlock:^(GCTThemeManager *obj, NSUInteger idx, BOOL * _Nonnull stop) {
        XCTAssertEqual(self.manager, obj, @&quot;GCTThemeManager is not single&quot;);
    }];
}

/*
 ** 测试开关
 */
- (void)testGCTThemeSwitch {
    XCTAssertFalse(self.manager.switchIsOpen, &quot;default switch is closed&quot;);
    
    [self.switchObject setGCTThemeSwitch:YES];
    [self.manager switchBaseOn:self.switchObject];
    XCTAssertTrue(self.manager.switchIsOpen, &quot;switch is open&quot;);
}

/*
 ** 初始化后数据是否正确
 */
- (void)testGCTThemeInitNoMockDataIsNotNil {
    XCTAssertNotNil(self.manager.skinData, &quot;default data is not to be nil&quot;);
    XCTAssertTrue(self.manager.skinData.count != 0, &quot;default data is not to be nil&quot;);
    XCTAssertNotNil(self.manager.tempSkinData, &quot;temp data is not to be nil&quot;);
    XCTAssertTrue(self.manager.tempSkinData.count != 0, &quot;temp data is not be nil&quot;);
}

/*
 ** 通过key来获取图片
 */
- (void)testGCTThemeGetImageByKey {
    XCTAssertNil([self.manager getImageByKey:@&quot;&quot; from:GCTThemeModuleTypeHomeTabbar], &quot;key is nil image should be nil&quot;);
    XCTAssertNil([self.manager getImageByKey:@&quot;image_home_button_selected_error&quot; from:GCTThemeModuleTypeHomeTabbar], &quot;key error image should be nil&quot;);
    XCTAssertNil([self.manager getImageByKey:@&quot;image_home_button_selected&quot; from:GCTThemeModuleTypeHomeWifi], @&quot;type is error image should be nil&quot;);
    XCTAssertNotNil([self.manager getImageByKey:@&quot;image_home_button_selected&quot; from:GCTThemeModuleTypeHomeTabbar], &quot;key and type is right and data has value, image should not be nil&quot;);
}

/*
 ** 通过key来获取图片名字
 */
- (void)testGCTThemeGetImageNameByKey {
    XCTAssertEqual([self.manager getImageNameByKey:@&quot;image_home_button_selected&quot; from:GCTThemeModuleTypeHomeTabbar], @&quot;Home_Selected&quot;, &quot;image name should be equal&quot;);
}

/*
 ** 通过key来获取颜色
 */
- (void)testGetColorByKey {
    // 从春节主题中获取
    [self.switchObject setGCTThemeSwitch:YES];
    [self.manager switchBaseOn:self.switchObject];
    XCTAssertTrue([[self.manager getColorByKey:@&quot;color_before_background&quot; from:GCTThemeModuleTypeHomeMiddleContent] isEqual:[UIColor colorWithHexString:@&quot;#cccccc&quot;]], &quot;UIColor should be equal&quot;);
}

- (void)testGCTThemeGetTitleByKey {
    // 从春节主题中获取
    [self.switchObject setGCTThemeSwitch:YES];
    [self.manager switchBaseOn:self.switchObject];
    XCTAssertTrue([[self.manager getTitleByKey:@&quot;value_home_title&quot; from:GCTThemeModuleTypeHomeMiddleContent] isEqual:@&quot;测试&quot;], &quot;Title should be equal&quot;);
}
</code></pre>
<h3 id="思考篇">思考篇</h3>
<ul>
<li>配置的时机，需要切换，尽量不要放到<code>AppDelegate</code>中，影响启动时间。</li>
<li><code>GCTThemeManager</code>中的开关<code>GCTThemeSwitchObject</code>以及数据源<code>GCTThemeData</code>相应的属性都是同时又<code>Getter/Setter</code>方法的，<strong>数据不安全！！</strong></li>
<li><strong>！！都要通过字符串去查找，编码不是很友好！（后期会通过R.Obj去修正这个问题。）</strong></li>
<li>统一修改的时机每次启动只有一次，后期这里需要修改。</li>
</ul>
]]></content>
    </entry>
</feed>