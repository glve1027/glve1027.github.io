<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://glve1027.github.io/</id>
    <title>Ghcoder</title>
    <updated>2020-02-25T11:32:07.100Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://glve1027.github.io/"/>
    <link rel="self" href="https://glve1027.github.io/atom.xml"/>
    <subtitle>一个不断在修炼的程序员</subtitle>
    <logo>https://glve1027.github.io/images/avatar.png</logo>
    <icon>https://glve1027.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Ghcoder</rights>
    <entry>
        <title type="html"><![CDATA[关于链表LinkedList解题心得]]></title>
        <id>https://glve1027.github.io/post/guan-yu-lian-biao-linkedlistjie-ti-tao-lu/</id>
        <link href="https://glve1027.github.io/post/guan-yu-lian-biao-linkedlistjie-ti-tao-lu/">
        </link>
        <updated>2020-02-25T11:17:24.000Z</updated>
        <summary type="html"><![CDATA[<p>关于链表，近期在刷题的时候发现，它还是有操作的套路可言的，不见得所有的题目都能套上模板，但是已经能解决60-70%的链表体型了。套路模板如下：</p>
]]></summary>
        <content type="html"><![CDATA[<p>关于链表，近期在刷题的时候发现，它还是有操作的套路可言的，不见得所有的题目都能套上模板，但是已经能解决60-70%的链表体型了。套路模板如下：</p>
<!-- more -->
<ol>
<li>入参给你个<code>ListNode</code>, 也可以有其他参数，根据条件返回他指定类型的链表。</li>
<li>需要临界值的判断。</li>
<li>（可以建立一个dummp节点），为了简化操作</li>
<li>遍历循环 【4.1: 先报值保留下来  4.2:迭代】</li>
<li>返回<code>dummp.next</code>节点</li>
</ol>
<pre><code class="language-java">public ListNode xx(ListNode head, int val) {
        if (head == null) { return head; } // 如第二点
        // 这里是个假数据，简化判断用的 - 如第三点
        ListNode dummy = new ListNode(-1);
        ListNode node = dummy;
        while (head != null) {
            // 先把值保留下来 - 如4.1
            ListNode temp = head;
            // 在迭代 - 如4.2
            head = head.next;
            // 也可以根据条件提前返回

            if (temp.val != val) {
                node.next = temp;
                temp.next = null;

                node = node.next;
            }
        }
        return dummy.next; // 如5
    }
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://glve1027.github.io//post-images/1582629524044.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-java">class Solution {
    public ListNode deleteDuplicates(ListNode head) {
        if (head == null) { return head; }
        // 这里是个假数据，简化判断用的
        ListNode dummy = new ListNode(-1);
        ListNode node = dummy;
        int lastValue = -1;
        boolean first = true;
        while (head != null) {
            // 先把值保留下来
            ListNode temp = head;
            // 在迭代
            head = head.next;

            if (first || lastValue != temp.val) {
                node.next = temp;
                temp.next = null;
                if (first) { first = false; }

                lastValue = temp.val;
                node = node.next;
            }
        }
        return dummy.next;
    }
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://glve1027.github.io//post-images/1582629779038.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-java">/**
     * LeetCode 203
     * @param head
     * @param val
     * @return
     */
    public ListNode removeElements(ListNode head, int val) {
        if (head == null) { return head; }
        // 这里是个假数据，简化判断用的
        ListNode dummy = new ListNode(-1);
        ListNode node = dummy;
        while (head != null) {
            // 先把值保留下来
            ListNode temp = head;
            // 在迭代
            head = head.next;

            if (temp.val != val) {
                node.next = temp;
                temp.next = null;

                node = node.next;
            }
        }
        return dummy.next;
    }
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://glve1027.github.io//post-images/1582629898087.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-java">/**
     * LeetCode 206 翻转链表 - 利用之前节点的值重新创建节点拼接
     * @param head
     * @return
     */
    public ListNode reverseList(ListNode head) {
        if (head == null) { return null; }
        ListNode resultNode = null;
        while (head != null) {
            if (resultNode == null) {
                resultNode = new ListNode(head.val);
            } else {
                ListNode temp = new ListNode(head.val);
                temp.next = resultNode;

                resultNode = temp;
            }
            head = head.next;
        }
        return resultNode;
    }

    /**
     * LeetCode 206 翻转链表 - 不重新创建节点
     * @param head
     * @return
     */
    public ListNode reverseList_01(ListNode head) {
        if (head == null || head.next == null) return head;

        ListNode resultNode = null;
        while (head != null) {
            ListNode temp = head;

            head = head.next;

            if (resultNode == null) {
                temp.next = null;
                resultNode = temp;
            } else {
                temp.next = resultNode;
                resultNode = temp;
            }
        }
        return resultNode;
    }

    /**
     * LeetCode 206
     * @param head
     * @return
     */
    public ListNode reverseList_02(ListNode head) {
        if (head == null || head.next == null) { return head; }

        ListNode newHead = null;
        while (head != null) {
            ListNode temp = head.next;

            head.next = newHead;
            newHead = head;

            head = temp;
        }
        return newHead;
    }
    /**
     * LeetCode 206 递归的解法
     * @param head
     * @return
     */
    public ListNode reverseList_03(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode newNode = reverseList_03(head.next);
        head.next.next = head;
        head.next = null;
        return newNode;
    }
</code></pre>
<figure data-type="image" tabindex="4"><img src="https://glve1027.github.io//post-images/1582629978222.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-java">/**
     * LeetCode 237 删除一个节点
     * @param node
     */
    public void _237_deleteNode(ListNode node) {
        if (node.next == null) {
            node = null;
        } else {
            node.val = node.next.val;
            node.next = node.next.next;
        }
    }
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://glve1027.github.io//post-images/1582629595539.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-java">class Solution {
    public ListNode middleNode(ListNode head) {
        if (head == null || head.next == null) { return head; }
        ListNode slow = head;
        ListNode fast = head.next;
        while (fast != null) {
            if (fast.next == null) { return slow.next; }
            fast = fast.next.next;
            slow = slow.next;
        }
        return slow;
    }
}
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://glve1027.github.io//post-images/1582629688771.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-java"> /**
     * LeetCode 141 判断链表是否存在环
     * @param head
     * @return
     */
    public boolean _141_hasCycle(ListNode head) {
        if (null == head || null == head.next) { return false; }
        ListNode slow = head;
        ListNode fast = head.next;
        while (slow != null &amp;&amp; fast != null) {
            if (slow.equals(fast)) return true;
            if (fast.next == null) { return false; }
            fast = fast.next.next;
            slow = slow.next;
        }
        return false;
    }

    /**
     * LeetCode 141 配合HashMap解题
     * @param head
     * @return
     */
    public boolean _141_hasCycle_01(ListNode head) {
        if (null == head || null == head.next) { return false; }
        HashMap&lt;ListNode, Integer&gt; map = new HashMap&lt;&gt;();

        while (head != null) {
            if (map.containsKey(head)) { return true; }
            map.put(head, 1);
            head = head.next;
        }

        return false;
    }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[重拾算法(Java)]]></title>
        <id>https://glve1027.github.io/post/chong-shi-suan-fa-java/</id>
        <link href="https://glve1027.github.io/post/chong-shi-suan-fa-java/">
        </link>
        <updated>2020-02-21T11:46:55.000Z</updated>
        <summary type="html"><![CDATA[<p>最近买了几本算法方面的数，都是Java写的，其实算法重在思维方式，其实用什么语言去实现是无所谓的，之前都是用Swift来实现的，正好抽个机会学习一下Java。</p>
]]></summary>
        <content type="html"><![CDATA[<p>最近买了几本算法方面的数，都是Java写的，其实算法重在思维方式，其实用什么语言去实现是无所谓的，之前都是用Swift来实现的，正好抽个机会学习一下Java。</p>
<!-- more -->
<h2 id="重拾算法java">重拾算法（Java）</h2>
<ul>
<li>通过Java实现一个动态数组的功能：</li>
</ul>
<pre><code class="language-java">package com.gh;

@SuppressWarnings(&quot;unchecked&quot;)
public class ArrayList&lt;E&gt; {
	/*
	 * 元素的数量
	 */
	private int size;
	
	/*
	 * 所有的元素
	 */
	private E[] elements;
	
	/*
	 * 默认容量
	 */
	private static final int DEFAULT_CAPACITY = 10;
	private static final int ELEMENT_NOT_FOUND = -1;
	
	/*
	 * 构造函数
	 */
	public ArrayList(int capacity) {
		elements = capacity &lt; DEFAULT_CAPACITY ? (E[]) new Object[DEFAULT_CAPACITY] : (E[]) new Object[capacity];
	} 
	
	/*
	 * 默认构造函数
	 */
	public ArrayList() {
		this(DEFAULT_CAPACITY);
	}
	
	/*
	 * 数组的容量
	 */
	public int size() {
		return size;
	}
	
	/*
	 * 数组是否为空
	 */
	public boolean isEmpty() {
		return size == 0;
	}
	
	/*
	 * 根据index返回元素
	 */
	public E get(int index) {
		checkIndex(index);
		return elements[index];
	}
	
	/*
	 * 设置新的值
	 */
	public E set(int index, E element) {
		E old = get(index);
		elements[index] = element;
		return old;
	}
	
	/*
	 * 查看元素在数组中的位置
	 */
	public int indexOf(E elememt) {
		if (null == elememt) {
			for (int i = 0; i &lt; size; i++) {
				if (elements[i] == null) return i;
			}
	 	} else {
			for (int i = 0; i &lt; size; i++) {
				/**
				 * 如果 elements[i] == element 这样比较的是两个内存地址是否相同
				 * 这里用.equals不会影响对int的判断
				 * 因为这里的element肯定不会为null, 可以放心使用
				 */
				if (elememt.equals(elements[i])) return i;
			}
		}
		return ELEMENT_NOT_FOUND;
	}
	
	/*
	 * 是否包含元素
	 */
	public boolean contains(E element) {
		return indexOf(element) != ELEMENT_NOT_FOUND;
	}
	
	/*
	 * 清除所有的元素
	 */
	public void clear() {
		// 清除指针内存
		for (int i = 0; i &lt; size; i++) {
			elements[i] = null;
		}
		size = 0;
	}
	
	/*
	 * 添加元素
	 */
	public void add(E element) {
		add(size, element);
	}
	
	/*
	 * 删除元素
	 */
	public E remove(int index) {
		checkIndex(index);
		E originValue = elements[index];
//		for (int i = index+1; i &lt; size; i++) {
//			elements[i-1] = elements[i];
//		}
		while(index + 1 &lt; size) {
			elements[index] = elements[index+1];
			index ++;
		}
		// 1. 先减减 2. 再清空数据
		elements[--size] = null;
		return originValue;
	}
	
	/*
	 * 在某和节点位置添加元素
	 */
	public void add(int index, E element) {
		if (index &lt; 0 || index &gt; size) return;
		
		ensureCapacity(size + 1);
		
		for (int i = size - 1; i &gt;= index ; i--) {
			elements[i + 1] = elements[i];
		}
		elements[index] = element;
		size ++; 
	}
	
	@Override
	public String toString() {
		StringBuilder string = new StringBuilder();
		string.append(&quot;size=&quot;).append(size).append(&quot;, [&quot;);
		for (int i = 0; i &lt; size; i++) {
			string.append(elements[i]);
			if (i != size - 1) { 
				string.append(&quot;, &quot;);
			}
		}
		string.append(&quot;]&quot;);
		return string.toString();
	}
	
	/**
	 * 保证要有的capacity的容量
	 * @param capacity
	 */
	private void ensureCapacity(int capacity) {
		int oldCapacity = elements.length;
		if (oldCapacity &gt;= capacity) return;
		// 新的容量是旧容量的1.5倍
		int newCapacity = oldCapacity + (oldCapacity &gt;&gt; 1);
		
		E[] newElements = (E[]) new Object[newCapacity];
		for (int i = 0; i &lt; size; i++) {
			newElements[i] = elements[i];
		}
		elements = newElements;
		
		System.out.println(oldCapacity + &quot;扩容为:&quot; + newCapacity);
	}
	
	/*
	 * 检测Index的合法性
	 */
	private void checkIndex(int index) {
		if (index &lt; 0 || index &gt;= size) {
			throw new IndexOutOfBoundsException(&quot;数组越界 Index = &quot; + index + &quot;, Size = &quot; + size);
		}
	} 
}
</code></pre>
<blockquote>
<p>查看<code>Java</code>源码中可以发现，它的数组动态扩容用的是<code>1.5</code>倍, 这里的写法可以借鉴一下，很赞：<code>int newCapacity = oldCapacity + (oldCapacity &gt;&gt; 1);</code>运用位运算来来实现。</p>
</blockquote>
<ul>
<li>发现一个一直以来自己的不会的一个<code>Vim</code>命令,看源码的时候，我们正常需要<code>gd</code>到某个函数的定义或者实现里面去，这里就可以使用<code>control+o</code>就可以返回回去了【记录一下】。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[学习优秀的iOS代码(AFNetworking)]]></title>
        <id>https://glve1027.github.io/post/xue-xi-you-xiu-de-ios-dai-ma-afnetworking/</id>
        <link href="https://glve1027.github.io/post/xue-xi-you-xiu-de-ios-dai-ma-afnetworking/">
        </link>
        <updated>2020-02-15T11:35:32.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="学习优秀的代码总是能让我们得到不同的反思afnetworking一直作为网络请求的标杆很多ios开发者不知道nsurlsession但是他肯定知道afnetworking">学习优秀的代码总是能让我们得到不同的反思，AFNetworking一直作为网络请求的标杆，很多iOS开发者不知道NSURLSession，但是他肯定知道AFNetworking。</h2>
]]></summary>
        <content type="html"><![CDATA[<h2 id="学习优秀的代码总是能让我们得到不同的反思afnetworking一直作为网络请求的标杆很多ios开发者不知道nsurlsession但是他肯定知道afnetworking">学习优秀的代码总是能让我们得到不同的反思，AFNetworking一直作为网络请求的标杆，很多iOS开发者不知道NSURLSession，但是他肯定知道AFNetworking。</h2>
<!-- more -->
<ul>
<li><code>AFHTTPSessionManager</code></li>
</ul>
<blockquote>
<p>如何在<code>NSURL</code>中添加<code>/</code>的字符：<br>
添加<code>/</code>的原因：</p>
</blockquote>
<ol>
<li><code>重定向</code> - <code>如果没有/就会重定向，这样会浪费带宽</code></li>
<li>表示目录、信息的拼接</li>
<li>SEO</li>
</ol>
<pre><code class="language-objectivec">NSURL *url = [NSURL URLWithString:@&quot;www.xxx.com&quot;];
if (![[url absoluteString] hasSuffix:@&quot;/&quot;]) { url = [url URLByAppendingPathComponent:@&quot;&quot;]; }
</code></pre>
<ul>
<li>配置文件的东西，都是统一写在父类<code>AFURLSessionManager</code>中</li>
</ul>
<pre><code class="language-objectivec">// 所有的子类需要创建的时候，是这样创建的
- (instancetype)initWithBaseURL:(NSURL *)url sessionConfiguration:(NSURLSessionConfiguration *)configuration
{
    self = [super initWithSessionConfiguration:configuration]; // 统一在父类中配置
    if (!self) {
        return nil;
    }
    // ...
}
</code></pre>
<ul>
<li>在创建<code>NSURLSession</code>的时候，需要注意<code>delegateQueue</code>必须是串行的</li>
</ul>
<blockquote>
<p>queue: An operation queue for scheduling the delegate calls and completion handlers. The queue should be a serial queue, in order to ensure the correct ordering of callbacks. If nil, the session creates a serial operation queue for performing all delegate method calls and completion handler calls.</p>
</blockquote>
<pre><code class="language-objectivec">_session = [NSURLSession sessionWithConfiguration:self.sessionConfiguration delegate:self delegateQueue:self.operationQueue];
</code></pre>
<ul>
<li>在初始化<code>AFURLSessionManager</code>的时候，为了重新初始化<code>session</code>, 还存在一些之前的请求未完成，所以可以根据<code>Session</code>来获取相应的请求：</li>
</ul>
<pre><code class="language-objectivec">[self.session getTasksWithCompletionHandler:^(NSArray *dataTasks, NSArray *uploadTasks, NSArray *downloadTasks) {
    // 获取之前的请求
    for (NSURLSessionDataTask *task in dataTasks) {
        [self addDelegateForDataTask:task uploadProgress:nil downloadProgress:nil completionHandler:nil];
    }

    for (NSURLSessionUploadTask *uploadTask in uploadTasks) {
        [self addDelegateForUploadTask:uploadTask progress:nil completionHandler:nil];
    }

    for (NSURLSessionDownloadTask *downloadTask in downloadTasks) {
        [self addDelegateForDownloadTask:downloadTask progress:nil destination:nil completionHandler:nil];
    }
}];
</code></pre>
<ul>
<li>请求头参数的封装：<code>只会有一份、并且是全局的、并且线程安全</code>代码如下：</li>
</ul>
<pre><code class="language-objectivec">static NSArray * AFHTTPRequestSerializerObservedKeyPaths() {
    static NSArray *_AFHTTPRequestSerializerObservedKeyPaths = nil;
    static dispatch_once_t onceToken;
    dispatch_once(&amp;onceToken, ^{
        _AFHTTPRequestSerializerObservedKeyPaths = @[NSStringFromSelector(@selector(allowsCellularAccess)), NSStringFromSelector(@selector(cachePolicy)), NSStringFromSelector(@selector(HTTPShouldHandleCookies)), NSStringFromSelector(@selector(HTTPShouldUsePipelining)), NSStringFromSelector(@selector(networkServiceType)), NSStringFromSelector(@selector(timeoutInterval))];
    });

    return _AFHTTPRequestSerializerObservedKeyPaths;
}
</code></pre>
<ul>
<li>关闭自动KVO，手动触发</li>
</ul>
<pre><code class="language-objectivec">// 关闭KVO
+ (BOOL)automaticallyNotifiesObserversForKey:(NSString *)key {
    if ([AFHTTPRequestSerializerObservedKeyPaths() containsObject:key] {
        return NO;
    }

    return [super automaticallyNotifiesObserversForKey:key];
}

// 手动触发KVO
- (void)setCachePolicy:(NSURLRequestCachePolicy)cachePolicy {
    [self willChangeValueForKey:NSStringFromSelector(@selector(cachePolicy))];
    _cachePolicy = cachePolicy;
    [self didChangeValueForKey:NSStringFromSelector(@selector(cachePolicy))];
}
</code></pre>
<blockquote>
<p>这里关于如何在配置选项中设置值的操作还是挺不错的，运用响应式的操作模式。【具体可以详见类<code>AFHTTPRequestSerializer</code>中的第377行左右，运用KVO设置属性值的操作】</p>
</blockquote>
<ul>
<li>关于如何将<code>NSDictionary</code>的数据结构转化为<code>GET请求中需要拼接在URL中</code>/<code>POST请求中需要将请求放入的请求体中</code></li>
</ul>
<blockquote>
<p>核心的将<code>NSDictionary</code>转化为<code>NSArray</code>的数据的函数：</p>
</blockquote>
<pre><code class="language-objectivec">NSArray * AFQueryStringPairsFromKeyAndValue(NSString *key, id value) {
    
}
</code></pre>
<blockquote>
<p>上面的函数中运用了递归的方式来处理参数，有以下几点是亮点：</p>
</blockquote>
<ol>
<li>传入<code>NSDictionary</code>首先需要对key按照<code>ASCII</code>值进行排序，作者使用了<code>NSSortDescriptor</code>, 关于这个类的使用我下面给出一个范例。</li>
<li>具体讲数组中的对象拼接城<code>NSString *</code>的时候，核心的处理转译等细节处理放在函数<code>AFPercentEscapedStringFromString</code>中 【运用这点功能，其实我们就能实现一个自定义的路由功能】</li>
<li>这一点我有点没有想明白，作者为什么这里大量运用了<code>C语言函数</code>而不是<code>OC函数</code> 【！！不解】</li>
<li>关于<code>URL</code>对象中是否存在<code>?</code>，这个细节将决定我们改如何拼接之前已经处理好的参数，这里如果是我来处理的话，就会直接通过字符串的功能，直接查找的<code>?</code>这个符号，但是作者却没有这样使用，作者是这样的判断的：</li>
</ol>
<pre><code class="language-objectivec">mutableRequest.URL.query ? @&quot;&amp;%@&quot; : @&quot;?%@&quot;, query
</code></pre>
<blockquote>
<p>关于query的解释如下：This property contains the query string. Any percent-encoded characters are not unescaped. If the receiver does not conform to RFC 1808, this property contains nil. For example, in the URL http://www.example.com/index.php?key1=value1&amp;key2=value2, the query string is key1=value1&amp;key2=value2.</p>
</blockquote>
<ul>
<li>关于<code>NSSortDescriptor</code>的使用，我写了个Demo:</li>
</ul>
<pre><code class="language-objectivec">// Person.h
@interface Person : NSObject
@property (strong, nonatomic) Salary *salary;
@property (copy, nonatomic) NSString *firstName;
@property (copy, nonatomic) NSString *lastName;
+ (instancetype)initWithFirstName:(NSString *)first lastName:(NSString *)last;
+ (instancetype)initWithFirstName:(NSString *)first lastName:(NSString *)last salary:(NSInteger)salary;
@end

// Person.m
@implementation Person
+ (instancetype)initWithFirstName:(NSString *)first lastName:(NSString *)last {
    Person *p = [Person new];
    p.firstName = first;
    p.lastName  = last;
    return p;
}

+ (instancetype)initWithFirstName:(NSString *)first lastName:(NSString *)last salary:(NSInteger)salary {
    Person *p = [Person initWithFirstName:first lastName:last];
    if (p != nil) {
        Salary *s = [Salary new];
        s.amount = salary;
        p.salary = s;
    }
    return p;
}

- (NSString *)description {
    return self.salary == nil ? [NSString stringWithFormat:@&quot;firstName:%@-lastName:%@&quot;, self.firstName, self.lastName] : [NSString stringWithFormat:@&quot;firstName:%@-lastName:%@-salary:%ld&quot;, self.firstName, self.lastName, (long)self.salary.amount];
}
@end

// Salary.h
@interface Salary : NSObject
@property (nonatomic, assign) NSInteger amount;
@end

// Test
NSMutableArray &lt;Person *&gt; *persons = [NSMutableArray new];
[persons addObject:[Person initWithFirstName:@&quot;1&quot; lastName:@&quot;100&quot;]];
[persons addObject:[Person initWithFirstName:@&quot;2&quot; lastName:@&quot;99&quot;]];
[persons addObject:[Person initWithFirstName:@&quot;98&quot; lastName:@&quot;3&quot;]];
[persons addObject:[Person initWithFirstName:@&quot;98&quot; lastName:@&quot;4&quot;]];
[persons addObject:[Person initWithFirstName:@&quot;97`&quot; lastName:@&quot;4&quot;]];
[persons addObject:[Person initWithFirstName:@&quot;96`&quot; lastName:@&quot;4&quot; salary:1]];
[persons addObject:[Person initWithFirstName:@&quot;96`&quot; lastName:@&quot;4&quot; salary:2]];

// 创建排序条件
NSSortDescriptor *sd1 = [[NSSortDescriptor alloc] initWithKey:@&quot;firstName&quot; ascending:YES];
NSSortDescriptor *sd2 = [[NSSortDescriptor alloc] initWithKey:@&quot;lastName&quot; ascending:NO];
NSSortDescriptor *sd3 = [[NSSortDescriptor alloc] initWithKey:@&quot;salary.amount&quot; ascending:NO]; // 这里的key, 类似于KVO中的键值d观察，可以具体观察某个对象的属性值

NSArray *sortedDatas = [persons sortedArrayUsingDescriptors:@[sd1, sd2, sd3]];

for (id data in sortedDatas) {
    NSLog(@&quot;%@&quot;, data);
}
</code></pre>
<ul>
<li>通过<code>NSURLSession</code>和<code>NSURLRequest</code>来创建<code>NSURLSessionDataTask</code>的过程，<code>AFNetworking</code>做了一个版本兼容的处理，操作如下：</li>
</ul>
<pre><code class="language-objectivec">__block NSURLSessionDataTask *dataTask = nil;
url_session_manager_create_task_safely(^{
    dataTask = [self.session dataTaskWithRequest:request];
});

static void url_session_manager_create_task_safely(dispatch_block_t _Nonnull block) {
    if (block != NULL) {
        if (NSFoundationVersionNumber &lt; NSFoundationVersionNumber_With_Fixed_5871104061079552_bug) {
            // Fix of bug
            // Open Radar:http://openradar.appspot.com/radar?id=5871104061079552 (status: Fixed in iOS8)
            // Issue about:https://github.com/AFNetworking/AFNetworking/issues/2093
            dispatch_sync(url_session_manager_creation_queue(), block);
        } else {
            block();
        }
    }
}

static dispatch_queue_t url_session_manager_creation_queue() {
    static dispatch_queue_t af_url_session_manager_creation_queue;
    static dispatch_once_t onceToken;
    dispatch_once(&amp;onceToken, ^{
        af_url_session_manager_creation_queue = dispatch_queue_create(&quot;com.alamofire.networking.session.manager.creation&quot;, DISPATCH_QUEUE_SERIAL);
    });

    return af_url_session_manager_creation_queue;
}
</code></pre>
<blockquote>
<p>因为<code>AFNetworking</code>的<code>taskIdentifier</code>和<code>回调代理</code>一一绑定的<br>
首先这里创建队列以及操作的函数作者还是封装成了<code>static</code>的函数<br>
可以看到这里是为了处理在iOS8以下版本下的一个bug，具体解释可以看这个<a href="https://github.com/AFNetworking/AFNetworking/issues/2093">issue</a>,解释如下：<br>
Summary:<br>
taskIdentifiers on an NSURLSessionDataTask should be unique per NSURLSession, as the documentation states. The attached projects demonstrates an issue where tasks that are created concurrently can end up with the same taskIdentifier.<br>
Steps to Reproduce:</p>
</blockquote>
<ol>
<li>Dispatch 100 data tasks on the default priority queue.</li>
<li>Monitor the taskIdentifiers returned by the task.</li>
<li>Observe that they are occasionally the same — likely, since the queue is concurrent, this is when two tasks end up getting created on different threads around the same time.<br>
Expected Results:<br>
Task Identifiers should be unique per NSURLSession, as the documentation states.<br>
Actual Results:<br>
Task Identifiers are not always unique. The popular AFNetworking framework depends on this uniqueness, and can end up calling incorrect completionHandlers.<br>
Version:<br>
iOS 7.1.1</li>
</ol>
<blockquote>
<p>简单来说就是在通过并行的方式来创建task的时候，会偶尔出现<code>taskIdentifier</code>和之前的一模一样，这样就会导致之前的回调如果还没有回调回来，就会将之前那个回调强制remove掉</p>
</blockquote>
<ul>
<li>回调代理<code>AFURLSessionManagerTaskDelegate</code>和task<code>NSURLSessionTask</code>形成绑定关系，但是这里有一点不是很确定【！！不确定，只是自己的猜测】</li>
</ul>
<blockquote>
<p>作者这里并没有直接监听task对象的<code>countOfBytesReceived</code>/<code>countOfBytesSent</code>/<code>countOfBytesExpectedToSend</code>等等【我看之前有个版本的AF是直接监听task的这些属性值】，而是现在是直接创建了一个<code>NSProgress</code>对象，然后每次在代理回调的时候，赋值。<br>
这里猜测，是因为有可能上传文件的<code>totalUnitCount</code>会一直发生变化，不是说起初设置了之后就不会再发生变化了。关于如何使用<code>NSProgess</code>,我写了个Demo,如下:</p>
</blockquote>
<pre><code class="language-objectivec">- (void)viewDidLoad {
    [super viewDidLoad];
    
    self.progress = [[NSProgress alloc] initWithParent:nil userInfo:nil];
    self.progress.totalUnitCount = 100;
    [self.progress addObserver:self forKeyPath:NSStringFromSelector(@selector(fractionCompleted)) options:NSKeyValueObservingOptionNew context:nil];
}

- (void)touchesBegan:(NSSet&lt;UITouch *&gt; *)touches withEvent:(UIEvent *)event {
    //self.progress.completedUnitCount += 10;// 这里就算超出了也没有问题，但是这里最好不要这么做，做一下判断
    if (self.progress.completedUnitCount &lt; self.progress.totalUnitCount) {
        self.progress.completedUnitCount = (self.progress.completedUnitCount + 10) &gt;= self.progress.totalUnitCount
                                            ? self.progress.totalUnitCount
                                            : self.progress.completedUnitCount + 10;
    }
}

- (void)observeValueForKeyPath:(NSString *)keyPath ofObject:(id)object change:(NSDictionary&lt;NSKeyValueChangeKey,id&gt; *)change context:(void *)context {
    if ([keyPath isEqualToString:NSStringFromSelector(@selector(fractionCompleted))]) {
        NSLog(@&quot;---&gt;%@&quot;, object);
    }
}
</code></pre>
<ul>
<li><code>AFNetworking</code>如何实现一个简单的<code>工作流</code></li>
</ul>
<blockquote>
<p>这里所谓的<code>工作流</code>,只是简单地顺序流程，例如：<code>流程A做完-&gt;流程B做完-&gt;流程C做完</code><br>
具体的操作就是类<code>NSInputStream</code>流的操作：【具体参照AFNetworking多表单的发送中类<code>AFHTTPBodyPart</code>的操作】</p>
</blockquote>
<pre><code class="language-objectivec">- (BOOL)transitionToNextPhase {
    // 保证在主线程操作
    if (![[NSThread currentThread] isMainThread]) {
        dispatch_sync(dispatch_get_main_queue(), ^{
            [self transitionToNextPhase];
        });
        return YES;
    }

    switch (_phase) {
        case AFEncapsulationBoundaryPhase:
            _phase = AFHeaderPhase; // 跳转的下一个流程
            break;
        case AFHeaderPhase:
            [self.inputStream scheduleInRunLoop:[NSRunLoop currentRunLoop] forMode:NSRunLoopCommonModes];
            [self.inputStream open];
            _phase = AFBodyPhase;  // 跳转的下一个流程
            break;
        case AFBodyPhase:
            [self.inputStream close];
            _phase = AFFinalBoundaryPhase;  // 跳转的下一个流程
            break;
        case AFFinalBoundaryPhase:
        default:
            _phase = AFEncapsulationBoundaryPhase;  // 跳转第一个流程，准备下一次开始流程
            break;
    }
    _phaseReadOffset = 0;

    return YES;
}
</code></pre>
<blockquote>
<p>那么关键来了，在什么时候，需要调用这个方法呢？</p>
</blockquote>
<pre><code class="language-objectivec">// 1. 初始化的时候
- (instancetype)init {
    //...xxx
    [self transitionToNextPhase];
    //...xxx
}

// 2. 关闭、错误的时候需要扭转状态
// 这里的代码写的很巧妙
// 枚举值如下
/*
typedef NS_ENUM(NSUInteger, NSStreamStatus) {
    NSStreamStatusNotOpen = 0,
    NSStreamStatusOpening = 1,
    NSStreamStatusOpen = 2,
    NSStreamStatusReading = 3,
    NSStreamStatusWriting = 4,
    NSStreamStatusAtEnd = 5,
    NSStreamStatusClosed = 6,
    NSStreamStatusError = 7
};
*/
if ([self.inputStream streamStatus] &gt;= NSStreamStatusAtEnd) {
    [self transitionToNextPhase];
}

// 3. 正常读完数据的时候需要扭转状态
if (((NSUInteger)_phaseReadOffset) &gt;= [data length]) {
    [self transitionToNextPhase];
}
</code></pre>
<blockquote>
<p>上面的扭转，写的是很巧妙，但是却不适用很复杂，多情况的扭转，类似状态机，但是还是值得借鉴。</p>
</blockquote>
<ul>
<li>如果通过多表单上传信息</li>
</ul>
<pre><code class="language-objectivec">AFHTTPSessionManager *manager = [AFHTTPSessionManager manager];
manager.requestSerializer.timeoutInterval = 5;
manager.requestSerializer.allowsCellularAccess = 1;
AFJSONResponseSerializer *ser = [AFJSONResponseSerializer serializer];
manager.responseSerializer = ser;
ser.removesKeysWithNullValues = YES;// 遇到空值是否移除 null
NSDictionary *dic = @{ @&quot;name&quot;: @&quot;gh&quot;, @&quot;age&quot;: @(12), @&quot;info&quot;: @{ @&quot;address&quot;: @&quot;hangzhou&quot; }};
[manager POST:@&quot;https://www.baidu.com&quot; parameters:dic headers:@{} constructingBodyWithBlock:^(id&lt;AFMultipartFormData&gt;  _Nonnull formData) {
    /*
    // 在block中设置需要上传的文件
    NSString *path = [[NSBundle mainBundle] pathForResource:@&quot;1&quot; ofType:@&quot;png&quot;];
    // 将本地的数据拼接到formData中，指定name
    [formData appendPartWithFileURL:[NSURL fileURLWithPath:path] name:@&quot;file&quot; error:nil];
    
    // 或者使用这个接口拼接 指定name和filename
    NSData *picData = [NSData dataWithContentsOfFile:path];
    [formData appendPartWithFileData:picData name:@&quot;image&quot; fileName:@&quot;image.jpg&quot; mimeType:@&quot;image/jpeg&quot;];
    */
} progress:^(NSProgress * _Nonnull uploadProgress) {
        
} success:^(NSURLSessionDataTask * _Nonnull task, id  _Nullable responseObject) {
    
} failure:^(NSURLSessionDataTask * _Nullable task, NSError * _Nonnull error) {
    
}];
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[C中那些被遗忘的知识点：]]></title>
        <id>https://glve1027.github.io/post/c-zhong-na-xie-bei-yi-wang-de-zhi-shi-dian/</id>
        <link href="https://glve1027.github.io/post/c-zhong-na-xie-bei-yi-wang-de-zhi-shi-dian/">
        </link>
        <updated>2020-02-08T15:13:26.000Z</updated>
        <summary type="html"><![CDATA[<p>因为最近在看C++的知识点，发现很多C中的知识点有点不记得了，又把书拿出来翻了一下，整理了一些容易忽略的知识点。</p>
]]></summary>
        <content type="html"><![CDATA[<p>因为最近在看C++的知识点，发现很多C中的知识点有点不记得了，又把书拿出来翻了一下，整理了一些容易忽略的知识点。</p>
<!-- more -->
<ul>
<li>函数的<code>定义</code>和<code>实现</code>可以分类，定义的时候可以省略<code>形参的参数名</code>，例如下面的代码：</li>
</ul>
<pre><code class="language-c">int minus(int, int); // 定义
int minus(int a, int b) { return a - b; } // 实现
</code></pre>
<ul>
<li>
<p>如果函数只有<code>定义</code>，没有<code>实现</code>的时候, 编译器在编译的时候就会报错。</p>
</li>
<li>
<p>如果声明了一个<code>局部变量</code>, 但是没有<code>赋值</code>, 可能在不同的平台上会有不同的表现，目前在MAC OS上，会打印<code>0</code>, 而在Windows上则会直接报错，也会随机打印一个<code>垃圾数据</code>, 如果是全局<code>变量</code>, 例如<code>int a</code>, 默认会赋值<code>0</code>。</p>
</li>
<li>
<p><code>汉字</code>或者<code>字符串</code>, 必须要用字符数组来表示，因为<code>1个汉字</code> = <code>2个字符</code>。</p>
</li>
<li>
<p>同时使用两种修饰符</p>
</li>
</ul>
<pre><code class="language-c">signed short int a;
unsigned long long int a;
</code></pre>
<ul>
<li>
<p>在64位编译器环境下，short占2个字节(16位)，int占4个字节(32位)，long占8个字节(64位</p>
</li>
<li>
<p>signed代表有符号，包括正数、负数和0；unsigned代表无符号，只包括正数和0。比如，signed的取值范围是-32768<sub>32767，那么unsigned的取值范围是0</sub>65535</p>
</li>
<li>
<p>输出格式占位符号</p>
</li>
</ul>
<blockquote>
<p><code>%d</code> = <code>带符号的十进制形式输出整数（正数不带+）</code><br>
<code>%c</code> = <code>输出一个字符</code><br>
<code>%s</code> = <code>输出一个或者多个字符</code><br>
<code>%p</code> = <code>地址内存</code></p>
</blockquote>
<ul>
<li>
<p><code>printf(&quot;The price is %4d.&quot;, 14);</code> =&gt; <code>4d</code>可能会有空格来控制宽度（从左边开始算宽度） 【<code>-4d</code>则会从右边开始控制宽度】</p>
</li>
<li>
<p><code>printf(&quot;My height is %f&quot;, 179.95f);</code>默认会输出6位小数 【控制小数的位数<code>%8.2f</code> =&gt; <code>2位小数</code>，<code>宽度为8</code>】</p>
</li>
<li>
<p>逗号表达式：<code>表达式1, 表达式2, … …, 表达式n</code> = 【从左到右的顺序，先计算表达式1，接着计算表达式2，...，最后计算表达式n，<code>整个逗号表达式的值是最后一个表达式的值</code>】</p>
</li>
<li>
<p>数组的定义： <code>类型 数组名[数组个数]</code></p>
</li>
</ul>
<blockquote>
<p>数组的个数也可以是常量表达式<code>int a[3+4]</code>, 也可以是字符常量<code>int a['A']</code><br>
<code>a = &amp;a[0]</code><br>
<code>初始化: 类型 数组名[数组个数] = { 元素1，元素2 ...}</code> = <code>int a[2] = { 2, 3 }</code><br>
<code>c语言中对数组的越界不会检查</code><br>
当数组都赋值了，可以省略个数<code>int a[] = { 1, 2, 3 }</code>✅<br>
<code>int a[3] = {1,2,3};</code> =&gt; <code>a是数组名，代表数组的地址，它是常量</code><br>
二维数组<code>a[1][1]</code>中<code>a[0]、a[1]</code>也是数组，是一维数组，而且<code>a[0]、a[1]</code>就是数组名，因此<code>a[0]、a[1]</code>就代表着这个<code>一维数组的地址</code></p>
</blockquote>
<ul>
<li><code>puts</code>函数的使用</li>
</ul>
<pre><code class="language-c">char name[] = &quot;gonghuan&quot;
puts(name) // 从name的首地址开始一直输出到`\0`为止
</code></pre>
<ul>
<li><code>scanf</code>函数</li>
</ul>
<pre><code class="language-c">char name[10];
scanf(&quot;%s&quot;, name) // !! 这里不需要&amp;name 因为name就已经代表了数组的地址了
</code></pre>
<ul>
<li><code>putchar</code>字符输出</li>
</ul>
<pre><code class="language-c">putchar(65) = A
putchar('A') = A
</code></pre>
<ul>
<li>指针的定义：<code>类名标识符 * 指针变量名</code></li>
</ul>
<pre><code class="language-c">int a = 10; // 定义一个int变量a
int *p; // 定义一个指针类型p
p = &amp;a; // 将变量a的地址赋值给指针变量p
</code></pre>
<ul>
<li>指针赋值</li>
</ul>
<pre><code class="language-c">int a = 10;
int *p = &amp;a; // 将变量a的地址复制给指针变量p
*p = 9;      // 通过p存储的地址值，找到地址指向的具体指，将指修改为9, 同时a也就为9

int value = *p; // *p的意思是：根据p值(即变量a的地址)访问对应的存储空间，并取出存储的内容(即取出变量a的值)，赋值给value
</code></pre>
<ul>
<li>遍历数组</li>
</ul>
<pre><code class="language-c">int a[4] = { 1, 2, 3, 4 };
    
for (int i = 0; i &lt; 4 ; i++) {
    printf(&quot;%p\n&quot;, &amp;a[i]);
}
    
printf(&quot;====\n&quot;);
    
int *p = a;
for (int i = 0 ; i &lt; 4; i++) {
    printf(&quot;%d\n&quot;, *p); // 1,2,3,4
    p +=1;
}
    
printf(&quot;====\n&quot;);
for (int i = 0; i &lt; 4; i++) {
    printf(&quot;%p---%d\n&quot;, a+i, *(a+i));
}
</code></pre>
<ul>
<li>字符数组的遍历</li>
</ul>
<pre><code class="language-c">char names[3] = { 'g', 'h', '\0' };
char *p = names;
    
for (; *p != '\0'; p++) {
    printf(&quot;%c\n&quot;, *p);
}
</code></pre>
<ul>
<li>定义字符</li>
</ul>
<pre><code class="language-c">char *names = &quot;gh&quot;;
char name[] = &quot;gh&quot;;
char name[3] = { 'g', 'h', '\0' };
</code></pre>
<ul>
<li>下面这样赋值是错误的</li>
</ul>
<pre><code class="language-c">char name[3];
name = &quot;gh&quot;; // 这样是❌，因为name是数组的首地址，是常量，不能赋值
</code></pre>
<ul>
<li>字符的注意点：</li>
</ul>
<pre><code class="language-c">char a[] = &quot;gh&quot;;// 定义的是一个字符串变量！
char *p2 = &quot;gh&quot;;// 定义的是一个字符串常量！
</code></pre>
<ul>
<li>预编译的时候判断是否定义过某个宏</li>
</ul>
<pre><code class="language-c">#if defined(xxx)
...code...
#endif

// 等效
#ifdef xxx
#endif

// 未定义
#ifndef xxx // =&gt; (等效) #if !defined(xxx)
#endif
</code></pre>
<ul>
<li>自动变量<code>auto</code></li>
</ul>
<blockquote>
<p>所有的局部变量在默认情况下都是自动变量<br>
在程序执行到声明自动变量的代码块(函数)时，自动变量才被创建</p>
</blockquote>
<ul>
<li>静态变量：</li>
</ul>
<blockquote>
<p>所有的<code>全局变量</code>都是静态变量<br>
被关键字<code>static</code>修饰的<code>局部变量</code>也是静态变量<br>
生命周期：静态变量在程序运行<code>之前</code>创建，在程序的整个运行期间<code>始终存在</code>，直到程序结束</p>
</blockquote>
<ul>
<li>哪些值可以存放在寄存器中</li>
</ul>
<blockquote>
<p>被关键字<code>register</code>修饰的<code>自动变量</code>都是寄存器变量<br>
只有<code>自动变量</code>才可以是寄存器变量，全局变量和静态局部变量不行<br>
寄存器变量只限于<code>int</code>、<code>char</code>和<code>指针</code>类型变量使用</p>
</blockquote>
<ul>
<li>外部函数：</li>
</ul>
<blockquote>
<p>不能存在同名的外部函数<br>
使用<code>extern</code>[ 同时也可以省略 ]<br>
如果在定义函数时省略extern，则隐含为<code>外部函数</code><br>
extern是用来声明<code>已经定义</code>过而且<code>能够访问</code>的变量<br>
extern可以用来声明一个全局变量，但是不能用来定义变量</p>
</blockquote>
<ul>
<li>内部函数 [静态函数]</li>
</ul>
<blockquote>
<p>使用<code>static</code>修饰<br>
这样该函数就只能在其定义所在的文件中使用<br>
如果在不同的文件中有同名的内部函数，则互不干扰</p>
</blockquote>
<ul>
<li>
<p>声明一个外部变量 =&gt; <code>extern int a;</code></p>
</li>
<li>
<p>定义一个变量 =&gt; <code>int a;</code></p>
</li>
<li>
<p>定义结构体的时候可以定义变量</p>
</li>
</ul>
<pre><code class="language-c">struct Student {
    char *name;
    int age;
} stu;

// 也可以省略
struct {
    char *name;
    int age;
} stu;

// 使用的时候一定要加上struct
struct Student stu; // 这里和c++ 有点不同
</code></pre>
<ul>
<li>结构体初始化</li>
</ul>
<pre><code class="language-c">struct Student stu = { &quot;22&quot;, &quot;gh&quot; };  // 大括号按循序赋值

// 下面这样初始化是错误的❌，应该分开赋值
struct Student stu;
stu = { &quot;gh&quot;, 12 };
</code></pre>
<ul>
<li>结构体数据的初始化</li>
</ul>
<pre><code class="language-c">struct {
    char *name;
    int age;
} stu[2] = { {&quot;M&quot;, 27}, {&quot;N&quot;, 30} };
</code></pre>
<ul>
<li>结构体当做函数参数</li>
</ul>
<blockquote>
<p>都是值传递<br>
不会影响之前的值</p>
</blockquote>
<ul>
<li>枚举的定义和结构体很相似</li>
</ul>
<pre><code class="language-c">//先定义枚举类型，再定义枚举变量
enum Season { spring, summer, autumn, winter };
enum Season s;
//定义枚举类型的同时定义枚举变量
enum Season { spring, summer, autumn, winter } s;
//省略枚举名称，直接定义枚举变量
enum {spring, summer, autumn, winter} s;
</code></pre>
<ul>
<li>typeof</li>
</ul>
<pre><code class="language-c">// 定义一个结构体，顺便起别名
typedef struct MyPoint {
    float x;
    float y;
} Point;

// 可以省略结构体名称
typedef struct {
    float x;
    float y;
} Point;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[整理C++笔记]]></title>
        <id>https://glve1027.github.io/post/zheng-li-xue-xi-cbi-ji/</id>
        <link href="https://glve1027.github.io/post/zheng-li-xue-xi-cbi-ji/">
        </link>
        <updated>2020-01-28T15:54:39.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="学习c-的重要性不言而喻春节在家没事就整理了一下c的知识点">学习C++ 的重要性不言而喻，春节在家没事就整理了一下C++的知识点。</h2>
]]></summary>
        <content type="html"><![CDATA[<h2 id="学习c-的重要性不言而喻春节在家没事就整理了一下c的知识点">学习C++ 的重要性不言而喻，春节在家没事就整理了一下C++的知识点。</h2>
<!-- more -->
<h2 id="学习c">学习C++</h2>
<h3 id="第一天">第一天</h3>
<ol>
<li>c++ 中是不能同时存在两个<code>main</code>函数。</li>
<li><code>getchar()</code> 等待键盘输入。</li>
<li>输出: <code>cout &lt;&lt; &quot;xx&quot;</code>。</li>
<li><code>endl</code>: 换行的意思。</li>
<li>键盘赋值给： <code>cin &gt;&gt; xx</code>。</li>
<li>C语言不支持函数重载。【重载 -&gt; 函数名相同，参数的个数，参数的类型，参数的顺序不同。】</li>
<li>extern &quot;C&quot;/ extern &quot;C&quot; { } -&gt; 按照C语言的方式编译。</li>
<li>extern &quot;C&quot;/ extern &quot;C&quot; { } 使用在函数声明的地方。</li>
<li>c++ 支持默认参数，【默认参数只能从右边开始】</li>
<li>c++ 函数中使用默认参数，既有<code>定义</code>，也有<code>实现</code>，只能将默认参数使用在<code>定义</code>中 【可是是<code>常量</code>，也可以是<code>全局变量</code>】</li>
</ol>
<h3 id="第二天">第二天</h3>
<ol>
<li><code>inline</code> -&gt; 1. 函数代码不多  2. 函数调用频率比较频繁【可以修饰函数<code>声明</code>、也可以修饰函数<code>实现</code>】</li>
<li><code>inline</code>和<code>宏</code>的区别：<code>inline</code>还是有<code>语法检测</code>和<code>函数检测</code></li>
<li><code>#pragma once</code>：防止整个文件内容被重复引入。【#ifndef,#define,#endif 不受任何编译器限制，也可以用于<code>部分代码</code>不被重复引用】</li>
<li>引用：1. <code>定义了一个引用,相当于变量的别名</code>, 2. <code>对引用做计算，就相当于对引用的变量做计算</code> 3. <code>定义引用的时候必须要赋值，不能修改指向 【对比指针更加安全】</code> 4. <code>不存在引用的引用/引用的指针/引用数组</code></li>
</ol>
<pre><code class="language-c++">int a = 10;
int b = 20;

int *p = &amp;a;  // 相当于p的指针指向变量a
int *&amp;rp = p; // 创建一个指针p的引用/别名rp
rp = &amp;b;      // rp的指针指向变量b/相当于变量b的指针赋值给p

// 数组引用
int array[] = { 10, 20, 30 };
int (&amp;rArray)[3] = array;
</code></pre>
<blockquote>
<p><code>const</code>: 1. 常量的意思，被修饰的变量不可修改。 2. 如果修饰的是<code>类</code>,<code>结构体（的指针）</code>其他的成员也不可以修改。3. 修饰的是其右边的内容 4. const 必须放在&amp;左边才能算是<code>常引用</code> 5. <code>常引用</code>可以用来修饰临时变量/表达式/函数返回值</p>
</blockquote>
<pre><code class="language-c++">int age = 10;
const int *p0 = &amp;age; // *p0是常量  p0不是常量
int const *p1 = &amp;age; // *p1是常量  p1不是常量
int * const p2 = &amp;age; // *p2不是常量，p2是常量
const int * const p3 = &amp;age; // *p3是常量，p3是常量

//
int age = 10;
int&amp; const rage = age; // rage 的指向不能改 【不管这里加不加`const`, 引用都不能再次赋值】
int const&amp; rage1 = age;// 不能通过引用修改所指向的内容 【因为rage的时候不需要添加&amp;】
rage = 30;


int const *page = &amp;age;  // 不能通过指针修改所指向的内容
int* const page1 = &amp;age; // 不能修改指针的指向，但是可以通过指针修改指向的内容
</code></pre>
<blockquote>
<p>函数参数用<code>const</code>修饰,入参可以是<code>const</code>， 也可以不是<code>const</code>.<br>
函数参数不用<code>const</code>修饰, 入参不可以<code>const</code></p>
</blockquote>
<h3 id="第三天">第三天</h3>
<ul>
<li>汇编基础</li>
</ul>
<pre><code class="language-c++">// Intel / AT&amp;T汇编
// 寄存器是在CPU里面的
// x86(32bit)  eax/ebx/ecx/edx/ebp...
// x86(64bit) |63-56|55-48|47-40|39-32|31-24|23-16|15-8|7-0|
//								                  |AH..|AL.|
//		                                          |AX......|
//									  |EAX.................|
//		      |RAX.........................................|
</code></pre>
<blockquote>
<p><code>R开头的寄存器是64位的，占8个字节</code>/ <code>E开头的寄存器是64位的，占4个字节</code><br>
[地址值]<br>
<code>word</code> = <code>2</code>个字节  <code>dword</code> = <code>4</code>个字节(double)  <code>qword</code> = <code>8</code>个字节(quad)<br>
<code>call</code>调用函数<br>
lea dest [地址值]</p>
</blockquote>
<pre><code class="language-c++">move eax, [0x12313..] // 将从地址 0x12313..取四个字节的值赋值给eax
lea dest [0x12313..]  // 将地址 0x12313..  赋值给 dest
</code></pre>
<blockquote>
<p><code>ret</code>函数返回<br>
<code>xor</code>异或<br>
<code>inc</code>自增/ <code>dec</code>自减<br>
<code>j</code>开头的一般都是跳转，大多数的带条件的跳转，一般跟test/cmp等指令配合使用</p>
</blockquote>
<pre><code class="language-c++">jmp 0x12129.. // 无条件的跳转
// 有条件的跳转
cmp eax, ebx
jz 0x12129..
mov eax, 1
mov ebx, 2
</code></pre>
<blockquote>
<p>开始开始调用前，<code>栈</code>空间已经分配好了，<code>栈</code>内存的特点：1. 由<code>CPU</code>自动创建和回收的</p>
</blockquote>
<blockquote>
<p>从内存地址开始往高地址走，取几个字节，就是取到的值<br>
一个变量的地址值是它所有字节中最小的地址值</p>
</blockquote>
<blockquote>
<p><code>引用</code>的本质就是<code>地址</code><br>
<code>引用不能修改</code>如何实现？ 【C++的规则】</p>
</blockquote>
<pre><code class="language-c++">int age = 10;
int &amp;rage = age; // 相当于 int * const rage = &amp;age 作用一样
</code></pre>
<blockquote>
<p><code>当引用指向了不同类型的数据时，会产生临时变量，即引用指向的并不是初始化的那个变量</code><br>
<code>表达式</code>可以被<code>赋值</code> =&gt; <code>(a = b) = 4;</code></p>
</blockquote>
<h3 id="第四天">第四天</h3>
<blockquote>
<p>C++中可以使用<code>struct</code>/<code>class</code>定义类.<br>
<code>struct</code>默认属性都是<code>public</code><br>
<code>class</code>默认属性都是<code>private</code></p>
</blockquote>
<pre><code class="language-c++">struct Person {
	// 成员变量
	int m_id;
	int m_age;
	int m_height;

	// 成员函数
	void run() {
		// this指向Person对象的指针
		// this里面存储的就是person对象的地址值
		this-&gt;m_id = 11;
		this-&gt;m_age = 22;
		this-&gt;m_height = 33;
	};
};

int main() {

	// 在栈空间分配了内存给person对象
	// 这个person的对象的内存会自动回收，不需要开发人员去管理
	// person对象占4个字节
	Person person;
	person.m_id = 1;
	person.m_age = 2;
	person.m_height = 3;
	person.run(); // 函数代码不占用空间【在编译的完成的时候就已经决定了函数的地址，调用函数直接就是call xxx】

	cout &lt;&lt; sizeof(person) &lt;&lt; endl; // 12个字节
	/*
	* 属性值在对象内的分布是连续的，分别占4个字节
	*/
	cout &lt;&lt; &amp;person &lt;&lt; endl;
	cout &lt;&lt; &amp;person.m_id &lt;&lt; endl;
	cout &lt;&lt; &amp;person.m_age &lt;&lt; endl;
	cout &lt;&lt; &amp;person.m_height &lt;&lt; endl;

	/*
	* 1.  取出person 的对象地址，存入到pPerson
	* 2.  取出pPerson也就是m_id的地址进行赋值
	* 3/4 取出pPerson的地址偏移4/8个字节，进行赋值
	*/
	Person* pPerson = &amp;person;
	pPerson-&gt;m_id = 4;
	pPerson-&gt;m_age = 5;
	pPerson-&gt;m_height = 6;

	return 0;
}
</code></pre>
<h3 id="第五天">第五天</h3>
<pre><code class="language-c++">
struct Person {
private:
	int m_age;

public:
	void setAge(int age) {
		// 过滤的操作
		if (age &lt; 0) return;
		// 不能通过.来获取值，例如this.xx 这是不对的
		this-&gt;m_age = age;
	}

	int getAge() {
		return this-&gt;m_age;
	}
};

class Person {// struct 只有成员访问权限的区别，其他没有区别
public:
	int m_age;
	void run() {
		cout &lt;&lt; &quot;run() &quot; &lt;&lt; endl;
	};
};

/* 内存空间的分布
1. 栈空间 【自动回收和分配】
2. 堆空间 【需要我们主动申请和释放】
3. 代码区
4. 全局区 【全局变量，特点：整个程序运行过程中都存在】
*/
</code></pre>
<blockquote>
<p>在c++的堆空间如何创建内存？</p>
</blockquote>
<pre><code class="language-c++">/*
1. malloc -&gt; free
2. new -&gt; delete
3. new [] -&gt; delete []
*/

void test() {
	// 申请4个字节的堆空间内存
	int* p = (int*)malloc(4);
	*p = 10;
	cout &lt;&lt; *p &lt;&lt; endl;
	free(p);
}

void test2() {
	int* p = new int;
	delete p;
};

void test3() {
	// 申请10个连续的int类型
	int* p = (int*)malloc(sizeof(int) * 10);
	free(p);

	int* p1 = new int[10];
	delete[] p1;
};

// 下列代码会出现内存泄漏
void test4() {
	int* p = new int; // 第一个int *的指针会出现内存泄漏
	p = new int;

	delete p;
};

void test5() {
	int size = sizeof(int);
	int* p = (int*)malloc(size); // 申请完内存之后，*p的值是脏数据
	// memory set =&gt; [从p地址开始的4个字节, 每个字节都存放0]
	memset(p, 0, size);

	cout &lt;&lt; *p &lt;&lt; endl;

	free(p);
};

void test6() {
	int* p1 = new int; // 没有初始化
	int* p2 = new int(); // 初始化为0
	int* p3 = new int(5); // 初始化为5
	int* p4 = new int[3]; // 没有初始化
	int* p5 = new int[3](); // 初始化为0
	int* p6 = new int[3]{}; // 初始化为0
	int* p7 = new int[3]{ 5 }; // 初始化为 5，0，0
};

struct Person {
	int m_age;
};

// 全局区
Person g_person;

int main() {
	// 栈空间
	Person person;

	// 堆空间
	Person* p = new Person();
	p-&gt;m_age = 20;
	// 释放空间
	delete p;

	Person* p1 = (Person*)malloc(sizeof(Person));
	// 释放空间
	free(p1);

	return 0;
}
</code></pre>
<blockquote>
<p>构造函数的注意事项：</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

struct Person {
	int m_age;

	Person() {
		cout &lt;&lt; &quot;Person() &quot; &lt;&lt; endl;
		this-&gt;m_age = 0;
		// 格式化Person中的所有值为0：memset(this, 0, sizeof(Person));
	};

	// 构造函数重载 overload
	// 一旦自定义了构造函数，一定要用自定义的构造函数初始化对象
	Person(int age) {
		this-&gt;m_age = age;
		cout &lt;&lt; &quot;Person(int age)&quot; &lt;&lt; endl;
	};
};

Person g_person(); // 这是一个函数声明

int main() {
	Person person3(); // 这是一个函数声明，无参，返回值类型为Person

	// 创建的person对象在栈空间
	Person person1;
	Person person(30);
	person.m_age = 20;
	cout &lt;&lt; &quot;age is &quot; &lt;&lt; person.m_age &lt;&lt; endl;

	// 创建的person对象在堆空间
	Person* person2 = new Person();
	delete person2;
	Person* person3 = new Person(100);
	delete person3;

	// 通过malloc创建对象内存的时候不会调用构造函数
	Person* person4 = (Person*)malloc(sizeof(Person));
	delete person4;

	// 在某些特定的情况下，编译器会为类生成空的无参的构造函数

	return 0;
};
</code></pre>
<blockquote>
<p>成员变量的初始化问题</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

struct Person {
	int m_age;
};

// 全局区（成员变量的初始值为0）【不管有没有实现构造函数，都会默认初始化成员变量】
Person g_person;

/*
！！如果自定义了构造函数，除了全局区，其他内存空间的成员变量默认都不会初始化，需要开发人员手动初始化
*/

//!! 1个字节  =&gt; 0000 0000 (8个bit)

int main() {
	// 栈空间（成员变量没有初始化）【不管有没有实现构造函数，都不会初始化成员变量】
	Person person;
	// 堆空间
	Person* p1 = new Person; // 成员变量没有初始化 【不管有没有实现构造函数，都不会初始化成员变量】
	Person* p2 = new Person(); // 成员变量有初始化 【如果没有定义构造函数则会初始化成员变量，如果定义了构造函数则不会初始化成员变量】

	cout &lt;&lt; g_person.m_age &lt;&lt; endl;
	cout &lt;&lt; p1-&gt;m_age &lt;&lt; endl;
	cout &lt;&lt; p2-&gt;m_age &lt;&lt; endl;

	return 0;
};
</code></pre>
<h3 id="第六天">第六天</h3>
<blockquote>
<p>析构函数的特点：</p>
</blockquote>
<pre><code class="language-c++">struct Person {
	int m_age;
	// 对象创建完毕的时候调用
	Person() {
		cout &lt;&lt; &quot;Person()&quot; &lt;&lt; endl;
		this-&gt;m_age = 0;
	};
	// 对象销毁的时候调用
	// 析构函数特点： 1. 不能重载 2. 使用malloc创建的内存，调用free, 不能触发析构函数 3. 构建函数/析构函数必要public
	~Person() {
		cout &lt;&lt; &quot;~Person()&quot; &lt;&lt; endl;
	};
};
</code></pre>
<blockquote>
<p>对象内存的管理</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

// 如果这个对象里面没有任何成员变量，也会占1个字节
struct Car {
	Car() {
		cout &lt;&lt; &quot;Car()&quot; &lt;&lt; endl;
	};

	~Car() {
		cout &lt;&lt; &quot;~Car()&quot; &lt;&lt; endl;
	};
};

struct Person {
	int m_age;  // 4个字节
	Car *m_car; // 一个指针，占4个字节，起初不会立即创建一个Car对象

	// 初始化工作
	Person() {
		cout &lt;&lt; &quot;Person created&quot; &lt;&lt; endl;
		this-&gt;m_car = new Car();
	};

	// 内存回收，清理工作（回收Person对象内部的申请的空间）
	~Person() {
		delete this-&gt;m_car;
		cout &lt;&lt; &quot;Person destoryed&quot; &lt;&lt; endl;
	};
};

int main() {
	Person* p = new Person(); // p指针在栈空间  Person/Car都在堆空间

	cout &lt;&lt; sizeof(Car) &lt;&lt; endl; // 啥属性都没有，也会占有1个字节

	delete p;

	return 0;
};
</code></pre>
<blockquote>
<p>声明和实现分离的操作<br>
<code>Person.h</code>文件  定义声明</p>
</blockquote>
<pre><code class="language-c++">#pragma once

// 声明
class Person {
	int m_age;
public:
	Person();
	~Person();
	void setAge(int age);
	int getAge();
};
</code></pre>
<blockquote>
<p><code>Person.cpp</code>文件  实现内容</p>
</blockquote>
<pre><code class="language-c++">#include &quot;Person.h&quot;
#include &lt;iostream&gt;

using namespace std;

// :: 域运算符号 【跟在函数名的前面】
// 实现
Person::Person() {
	cout &lt;&lt; &quot;Person()&quot; &lt;&lt; endl;
};
Person::~Person() {
	cout &lt;&lt; &quot;~Person()&quot; &lt;&lt; endl;
};
void Person::setAge(int age) {
	this-&gt;m_age = age;
};
int Person::getAge() {
	return this-&gt;m_age;
};
</code></pre>
<blockquote>
<p>命名空间的概念</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &quot;Person.h&quot;
using namespace G2;
using namespace std;


// 命名空间不影响内存布局
namespace GX {
	int g_no;
	class Person {
	public:
		int m_age;
	};
};


namespace GH {
	int no;
	class Person {
	public:
		int m_height;
	};

	void test() {

	};
};

void test1() {
	GX::Person person;
	person.m_age = 10;
	GX::g_no = 1;

	GH::Person person2;
	person2.m_height = 10;
	GH::Person* person3 = new GH::Person();
	person3-&gt;m_height = 11;
	GH::test();
};

// 命名空间可以嵌套
namespace GG {
	namespace HH {
		int m_age;
	};
};

void test11() {};

namespace G1 {
	int m_age;
};

namespace G1 {
	int m_height;
};

// =&gt; 合并命名空间, 和上面是等效的
namespace G1 {
	int m_age;
	int m_height;
};

int main() {
	GG::HH::m_age = 10;

	using namespace GG::HH;
	m_age = 11;

	::test11(); // 空的，就是最大的命名空间

	return 0;
};
</code></pre>
<blockquote>
<p>C++ 关于继承</p>
</blockquote>
<pre><code class="language-c++">// C++ 里面没有想Java/Object-C里面一样有基类
int main() {
	
	GoodStudent gs;
	gs.m_age = 20;
	gs.m_no = 1;
	gs.m_money = 2000;

	cout &lt;&lt; sizeof(Person) &lt;&lt; endl;
	cout &lt;&lt; sizeof(Student) &lt;&lt; endl;
	cout &lt;&lt; sizeof(GoodStudent) &lt;&lt; endl;

	// 内存排布
	// 父类的成员变量在前面，子类的成员变量在后面
	cout &lt;&lt; &amp;gs &lt;&lt; endl;
	cout &lt;&lt; &amp;gs.m_age &lt;&lt; endl;
	cout &lt;&lt; &amp;gs.m_no &lt;&lt; endl;
	cout &lt;&lt; &amp;gs.m_money &lt;&lt; endl;

	return 0;
};
</code></pre>
<blockquote>
<p>成员访问权限的问题：</p>
</blockquote>
<pre><code class="language-c++">/*
子类内部访问父类成员的权限，是取以下2个权限最小的那个一个
1. 成员本身的访问权限
2. 上一级父类的继承方式
*/
// 开发中的继承都是要用public关键字，这样可以保证父类的权限特性

struct Person {
protected: // 子类内部/当前类内部可以访问
	int m_age;
};

struct Student: public Person {
private:// 只有当前类内部可以访问（class默认）
	int m_score;
};

struct GoodsStudent: public Student {
public: // 任何地方都可以访问 （struct默认）
	int m_money;
};
</code></pre>
<blockquote>
<p>初始化列表</p>
</blockquote>
<pre><code class="language-c++">struct Person {
	int m_age;
	int m_height;

	Person() {
		// ！！直接调用构造函数，会创建一个临时对象，传入一个临时的地址给this指针
		// ！！构造函数的互相调用必须要在初始化列表中调用
		Person(0, 0);
		/*this-&gt;m_age = 0;
		this-&gt;m_height = 0;*/
	}

	// 初始化列表 : m_age(age), m_height(height) // 语法糖
	// 1. 只能用在构造函数中
	// 2. 初始化列表中的顺序只和 成员变量声明的顺序有关【跟内存的排布】
	Person(int age, int height) : m_age(age), m_height(height) {}
	/*Person(int age, int height) {
		this-&gt;m_age = age;
		this-&gt;m_height = height;
	}*/

	void display() {
		cout &lt;&lt; &quot;age is &quot; &lt;&lt; this-&gt;m_age &lt;&lt; endl;
		cout &lt;&lt; &quot;height is &quot; &lt;&lt; this-&gt;m_height &lt;&lt; endl;
	}
};
</code></pre>
<h3 id="第七天">第七天</h3>
<blockquote>
<p>构造函数列表的功能</p>
</blockquote>
<pre><code class="language-c++">class Person {
	int m_age;
	int m_height;
public:
	//Person(int age = 0, int height = 0) :m_age(age), m_height(height) {}
	// 定义，默认参数只能写在函数的声明中
	Person(int age = 0, int height = 0);
};

// 实现分离
// 构造函数的初始化列表只能写在实现中
Person::Person(int age, int height) :m_age(age), m_height(height) {}

int main() {
	Person person;
	Person person1(10);
	Person person2(10, 180);

	return 0;
};
</code></pre>
<blockquote>
<p>父类的构造函数<br>
子类默认会调用父类的无参构造函数<br>
子类默认会调用父类的无参构造函数<br>
子类的构造函数显式调用父类的有参构造函数，就不会区调用父类的无参构造函数<br>
如果父类里面没有无参的构造函数，子类必须调用有参的构造函数</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

// 子类默认会调用父类的无参构造函数
// 子类的构造函数显式调用父类的有参构造函数，就不会区调用父类的无参构造函数
// 如果父类里面没有无参的构造函数，子类必须调用有参的构造函数
class Person {
	int m_age;
public:
	//Person() {
	//	cout &lt;&lt; &quot;Person()&quot; &lt;&lt; endl;
	//};

	Person(int age) : m_age(age) {
		cout &lt;&lt; &quot;Person(int age)&quot; &lt;&lt; endl;
	}
};

class Student :public Person {
	int m_score;
public:
	Student(): Person(0) {
		cout &lt;&lt; &quot;Student()&quot; &lt;&lt; endl;
	}

	//Student(int age, int score): m_score(score), Person(age) {
	//	cout &lt;&lt; &quot;Student(int age, int score)&quot; &lt;&lt; endl;
	//}
};
</code></pre>
<blockquote>
<p>父类指针的问题</p>
</blockquote>
<pre><code class="language-c++">// 父类指针可以指向子类对象， 这个是安全的，开发中经常用到
Person* stu = new Student();
// 子类指针指向父类对象，这个是不安全的
Student* person = (Student *)new Person();
</code></pre>
<blockquote>
<p>如果子类继承父类的方式不是<code>public</code>, 那么就不能用父类指针指向子类对象<br>
多态的要素：1. 子类重写父类成员函数 2. 父类指针指向子类对象 3. 利用父类指针调用重写的成员函数【必须是虚函数】</p>
</blockquote>
<blockquote>
<p>虚表的内存排布</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

class Animal {
public:
	int m_age;
	virtual void speak() {
		cout &lt;&lt; &quot;Animal::Speak()&quot; &lt;&lt; endl;
	}
	virtual void run() {
		cout &lt;&lt; &quot;Animal::run()&quot; &lt;&lt; endl;
	}
};

class Cat : public Animal {
public:
	int m_life;
	void speak() {
		cout &lt;&lt; &quot;Cat::Speak()&quot; &lt;&lt; endl;
	}
	void run() {
		cout &lt;&lt; &quot;Cat::run()&quot; &lt;&lt; endl;
	}
};

int main() {

	Animal* cat = new Cat();
	cat-&gt;m_age = 20;
	cat-&gt;speak();
	cat-&gt;run();
	/*
	// 取出cat指针变量里面的存储的地址值
	// eax里面存放的是Cat对象的地址值
	mov         eax,dword ptr [cat]  

	// 取出cat对象的前面4个字节内容给edx
	// edx里面存储的是虚表的地址
	mov         edx,dword ptr [eax] 

	mov         esi,esp  
	mov         ecx,dword ptr [cat]  

	// 取出虚表地址（再偏移4个字节）取前4个字节的内容给eax
	// eax里面存储的是Cat::run的函数地址
	mov         eax,dword ptr [edx+4]  

	call        eax  
	*/

	/*
	同一个类的虚表的都是同一的
	*/
	Animal* a1 = new Animal(); // 虚表地址：84 9d f9 00 -&gt; 这个是小端(从右往左读)  0xf99d84
	Animal* a2 = new Animal(); // 虚表地址：84 9d f9 00
	Animal* a3 = new Animal(); // 虚表地址：84 9d f9 00

	return 0;
};
</code></pre>
<h3 id="第八天">第八天</h3>
<blockquote>
<p>如果子类中没有覆写父类的的虚函数，那么它存放的就是父类的虚函数地址。<br>
如果子类中没有任何覆写的函数，那么它还是存在虚表地址，子类和父类的虚表地址不一样，但是内容是一样的。<br>
如何调用父类中的成员函数。<br>
如果使用了多态，析构函数也要使用虚函数。这样才能保证正常销毁对象。<br>
如果子类继承了抽象类，没有<code>完全</code>覆盖虚函数，则子类也属抽象类</p>
</blockquote>
<pre><code class="language-c++">父类::xxx
</code></pre>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

// 含有纯虚函数的类，是抽象类 【不能直接实例化】
class Animal {
public:
	// 纯虚函数 【没有函数体的函数，且初始化为0, 定义接口规范】
	virtual void speak() = 0;
	virtual void run() = 0;
};

// 如果子类继承了抽象类，没有完全覆盖虚函数，则子类也属抽象类
class Dog : public Animal {
public:
	void speak() {
		cout &lt;&lt; &quot;Dog speak()&quot; &lt;&lt; endl;
	}

	void run() {
		cout &lt;&lt; &quot;Dog run()&quot; &lt;&lt; endl;
	}
};
</code></pre>
<blockquote>
<p>多继承<br>
如果子类继承的多个父类都有虚函数，那么子类对象就会产生对应的<code>多张虚表</code><br>
如果是同名函数，想要调用指定函数的话，就在指定函数名前面加 <code>对象::</code> [<code>如果自己没有函数的话，就会报错，因为二意性</code>]<br>
虚基类</p>
</blockquote>
<pre><code class="language-c++">/*
1. 虚表地址  ---------------------------&gt;  1. 0: 虚表指针与本类起始的偏移量 （一般为0）
2. m_score                                2. 8：虚基类成员与本类的偏移量
3. m_age    // 虚基类的属性值放在最后
*/
</code></pre>
<h3 id="第九天">第九天</h3>
<blockquote>
<p><code>static</code>相关知识<br>
静态成员：</p>
<ol>
<li>存储在数据段 （全局区，类似全局变量） 2. 整个程序运行过程中只有有一份内存<br>
与下面的全局静态变量相比，它可以设置访问权限（private,protected,public）,达到局部共享的目的</li>
<li>静态成员必须初始化  2. 必须在类外面初始化  3. 初始化的时候不能带static<br>
如果类的定义和实现都分开的话，需要将static定义的属性值初始化的是步骤放在.cpp中<br>
1.直接通过类来获取  2.直接通过对象来获取  3.直接通过指针来获取</li>
</ol>
</blockquote>
<hr>
<blockquote>
<p>静态成员函数：<br>
静态函数内部不能使用<code>this</code>（<code>this</code>指针只能用在非静态成员函数内部）<br>
不能用虚函数<br>
内部不能访问非静态成员变量，只能访问静态成员变量/函数<br>
非静态成员函数可以访问 静态成员函数和静态成员变量<br>
构造函数和析构函数不能是静态函数<br>
<code>声明</code>和<code>实现</code>分离的时候，<code>实现</code>中不能出现<code>static</code></p>
</blockquote>
<pre><code class="language-c++">class Person1 {
	static int getAge();
};

int Person1::getAge() {
	return 1;
}
</code></pre>
<pre><code class="language-c++">/*
	静态成员变量 
	1. 存储在数据段 （全局区，类似全局变量）
	2. 整个程序运行过程中只有有一份内存
*/

// 与下面的全局静态变量相比，它可以设置访问权限（private,protected,public）,达到局部共享的目的

/*
	1. 静态成员必须初始化
	2. 必须在类外面初始化
	3. 初始化的时候不能带static
*/

// 如果类的定义和实现都分开的话，需要将static定义的属性值初始化的是步骤放在.cpp中

int main() {
	// 1.直接通过类来获取
	Car::m_price = 30;
	// 2.直接通过对象来获取
	Car car3;
	cout &lt;&lt; car3.m_price &lt;&lt; endl;
	// 3.直接通过指针来获取
	Car* p = new Car();
	cout &lt;&lt; p-&gt;m_price &lt;&lt; endl;
}
</code></pre>
<blockquote>
<p>单例模式</p>
</blockquote>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

/*
	我们希望某些类的实例对象永远只有一个
	单例模式
	条件：

	1. 构造函数私有化
	2. 定义一个私有的静态成员变量指针，用于指向单例对象
	3. 提供一个公共的返回单例对象的静态成员函数
*/

#include &lt;iostream&gt;
using namespace std;

/*
	我们希望某些类的实例对象永远只有一个
	单例模式
	条件：

	1. 构造函数私有化
	2. 定义一个私有的静态成员变量指针，用于指向单例对象
	3. 提供一个公共的返回单例对象的静态成员函数
*/

class Rocket {
public:
	static Rocket* sharedRocket() {
		// p_thread
		if (ms_rocket == NULL) {
			ms_rocket = new Rocket();
		}
		return ms_rocket;
	}
	static void freeRocket() {
		if (ms_rocket == NULL) return;
		delete ms_rocket;
		ms_rocket = NULL; // 回收内存、抹掉数据
	}
private:
	static Rocket *ms_rocket;
	Rocket() {
		cout &lt;&lt; &quot;Rocket()&quot; &lt;&lt; endl;
	}
};

Rocket* Rocket::ms_rocket = NULL;

</code></pre>
<blockquote>
<p>如果子类父类中存在同名的<code>静态成员变量</code>，是可以的，并且不存在继承功能</p>
</blockquote>
<pre><code class="language-c++">class Person {
public:
	static int m_age;
};

int Person::m_age = 1;

class Student : public Person {
public:
	static int m_age;
};

int Student::m_age = 2;

int main() {
	Person* p1 = new Person();
	Student* s1 = new Student();
	
	/*
	Person location is 0094C034
   Student location is 0094C038
	*/
	cout &lt;&lt; &quot;Person location is &quot; &lt;&lt; &amp;p1-&gt;m_age &lt;&lt; endl;
	cout &lt;&lt; &quot;Student location is &quot; &lt;&lt; &amp;s1-&gt;m_age &lt;&lt; endl;

	return 0;
}
</code></pre>
<h3 id="第十天">第十天</h3>
<ul>
<li>const 成员</li>
</ul>
<blockquote>
<p>被<code>const</code>修饰的成员变量、非静态成员变量</p>
</blockquote>
<ul>
<li>const成员变量</li>
</ul>
<blockquote>
<p>必须初始化（类内部初始化），可以在声明的时候<code>直接</code>初始化赋值/也可以<code>初始化列表</code>赋值</p>
</blockquote>
<ul>
<li>const成员函数（非静态）</li>
</ul>
<blockquote>
<p>函数的<code>声明</code>和<code>实现</code>都必须要带<code>const</code><br>
不能在函数内部修改<code>当前</code>对象的成员变量（非<code>static</code>）<br>
内部只能调用<code>const</code>成员函数,<code>static</code>成员函数<br>
非<code>const</code>成员函数可以调用<code>const</code>成员函数<br>
函数通过添加<code>const</code>和<code>非const</code>，可以构成重载<br>
<code>非const</code>对象（指针）优先调用<code>非const</code>成员函数、<code>const</code>对象（指针）只能调用<code>const</code>成员函数</p>
</blockquote>
<ul>
<li>
<p>拷贝构造函数</p>
</li>
<li>
<p>如果函数的入参是字符常量</p>
</li>
</ul>
<blockquote>
<p><code>Car(int price, char *name = NULL) :m_price(price) {}</code></p>
</blockquote>
<ul>
<li>使用对象类型作为函数的参数或者返回值，可能会产生一些不必要的中间对象</li>
</ul>
<h3 id="第十一天">第十一天</h3>
<ul>
<li>匿名对象</li>
</ul>
<pre><code class="language-c++">// 匿名函数
Person().display();
</code></pre>
<ul>
<li>参数传入对象时：</li>
</ul>
<pre><code class="language-c++">Person person; // 构造一次
test1(person); // 作为参数传去进去的时候，会再创建一个Person对象（所以这里会有一个拷贝构造）

test1(Person()); // 调用匿名构造函数，只会调用一次构造函数，不会调用拷贝构造
</code></pre>
<ul>
<li>隐式构造</li>
</ul>
<pre><code class="language-c++">/*
	Person(int age), age = 10,this = 00EFFCE0 - 创建一个新的对象1
	Person(int age), age = 20,this = 00EFFC14 - 隐式构造创建一个临时对象2，并且把它的属性值赋值给对象1的属性值
	~Person(), age = 20,this = 00EFFC14       - 析构对象2
	~Person(), age = 20,this = 00EFFCE0       - 析构对象1
*/
Person person(10);  //!! 如果没有单参数构造函数，则会报错
person = 20; // 隐式构造：Person(20)

// 禁止隐式构造
explicit Person(int age) : m_age(age) {}
</code></pre>
<ul>
<li>编译器自动生成的构造函数</li>
<li>C++的编译器会在默些特定的情况下，为类自动生成无参的构造函数，例如：</li>
</ul>
<ol>
<li>成员变量在声明的同时进行了初始化。【需要进行赋值】</li>
<li>有定义虚函数。 【需要创建虚表】</li>
<li>虚继承其他类。 【需要创建虚表】</li>
<li>包含了对象类型的成员（如果是指针类型则不需要调用构造函数），且这个成员有构造函数 【需要调用子类的构造函数】</li>
<li>父类有构造函数。【需要先创建父类】</li>
</ol>
<blockquote>
<p>对象创建后，需要做一些额外操作时（比如内存操作，函数调用），编译器一般都会为其生成构造函数。</p>
</blockquote>
<ul>
<li>友元 [编译器行为]</li>
</ul>
<blockquote>
<p>如果将函数<code>A</code>（非成员函数）声明为类<code>C</code>的友元函数，那么函数<code>A</code>就能直接访问类<code>C</code>对象的所有成员。</p>
</blockquote>
<pre><code class="language-c++">class Point {
	friend Point add(const Point&amp;, const Point&amp;); // 定义友元函数 【全局的】
	friend class Math; // 定义友元类
private:
	int m_x;
	int m_y;
public:
	Point(int x, int y) :m_x(x), m_y(y) {}
	int getX() const { return this-&gt;m_x; }
	int getY() const { return this-&gt;m_y; }
};

Point add(const Point&amp; point1, const Point&amp; point2) {
	return Point(point1.m_x + point2.m_x, point1.m_y + point2.m_y);
}

class Math {
public:
	Point add(const Point&amp; point1, const Point&amp; point2) {
		return Point(point1.m_x + point2.m_x, point1.m_y + point2.m_y);
	}
};
</code></pre>
<ul>
<li>
<p>内部类/局部类【定义在函数内部】</p>
</li>
<li>
<p>运算符重载</p>
</li>
</ul>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

class Point {
	friend Point operator+(const Point&amp;, const Point&amp;);
private:
	int m_x;
	int m_y;
public:
	Point(int x, int y) : m_x(x), m_y(y) {}
	static void display(const Point &amp;point) {
		cout &lt;&lt; &quot;(x: &quot; &lt;&lt; point.m_x &lt;&lt; &quot;, y: &quot; &lt;&lt; point.m_y &lt;&lt; &quot;)&quot; &lt;&lt; endl;
	}
};

// 操作符重载
Point operator+(const Point &amp;p1, const Point &amp;p2) {
	return Point(p1.m_x + p2.m_x, p1.m_y + p2.m_y);
}

int main() {
	
	Point::display(Point(1, 2) + Point(2, 3)); // 等价 =&gt;  display(operator(Point(1, 2), Point(2, 3)));
	Point::display(operator+(Point(1, 2), Point(2, 3)));


	return 0;
}
</code></pre>
<ul>
<li>通过指针<code>this</code>获取对象 =&gt; '*this'</li>
<li><code>const</code>对象只能调用<code>const</code>的成员函数</li>
</ul>
<h3 id="第十二天">第十二天</h3>
<ul>
<li>操作符重载</li>
</ul>
<pre><code class="language-c++">// ++x
void operator++() {
	this-&gt;m_x++;
	this-&gt;m_y++;
}

// x++
void operator++(int) {
	this-&gt;m_x++;
	this-&gt;m_y++;
}
</code></pre>
<ul>
<li><code>a++</code>与<code>++a</code>运算符的汇编解析</li>
</ul>
<blockquote>
<p>a++ 会返回a以前的值进行运算，运算完毕之后才会让a的值加1。【不能进行赋值】<br>
++a 会直接让a的值加1，并且作为最新的a进行运算。【可以进行赋值】</p>
</blockquote>
<pre><code class="language-c++">int b = a++ + 8;
// eax = a
mov         eax,dword ptr [a]  
// eax += 8
add         eax,8  
// b = eax [此时a的值没有发生改变]
mov         dword ptr [b],eax  

// ecx = a
mov         ecx,dword ptr [a]  
// ecx += 1
add         ecx,1  
// a = ecx [a++] 
mov         dword ptr [a],ecx  


int c = ++a + 8;
// 先将a的值加1，然后覆盖a的值
// eax = a
mov         eax,dword ptr [a]  
// eax += 1
add         eax,1  
// a = eax [a++] 
mov         dword ptr [a],eax  

// ecx = a [a = 9]
mov         ecx,dword ptr [a]  
// ecx += 8
add         ecx,8  
// c = exc
mov         dword ptr [c],ecx 
</code></pre>
<ul>
<li>重载</li>
</ul>
<pre><code class="language-c++">// 操作符重载
Point operator+(const Point &amp;p1, const Point &amp;p2) {
	return Point(p1.m_x + p2.m_x, p1.m_y + p2.m_y);
}

// 重载 cout &lt;&lt; Point;
ostream&amp; operator&lt;&lt;(ostream&amp; cout, const Point&amp; point) {
	return cout &lt;&lt; &quot;(x: &quot; &lt;&lt; point.m_x &lt;&lt; &quot;, y: &quot; &lt;&lt; point.m_y &lt;&lt; &quot;)&quot;;
}
</code></pre>
<ul>
<li>字符串重载实战</li>
<li>重写<code>String</code>类</li>
</ul>
<pre><code class="language-c++">// String.h
#pragma once
#include &lt;iostream&gt;
using namespace std;

class String {
	friend ostream&amp; operator&lt;&lt; (ostream&amp;, const String&amp;);
public:
	String&amp; operator=(const char* cstring);
	String&amp; operator=(const String &amp;string);
	String(const String &amp;string);
	String(const char* cstring);
	~String();
private:
	char* m_cstring;
	String&amp; assign(const char* cstring);
};

// String.cpp
#include &quot;String.h&quot;

String::String(const char* cstring) {
	assign(cstring);
}

String::String(const String&amp; string) {
	assign(string.m_cstring);
}

String::~String() {
	assign(NULL);
}

String&amp; String::operator=(const String&amp; string) {
	return assign(string.m_cstring);
}

String&amp; String::operator=(const char* cstring) {
	return assign(cstring);
}

String&amp; String::assign(const char* cstring) {
	// 如果指向一样的堆空间
	if (this-&gt;m_cstring == cstring) return *this;

	// 如果传入进来的cstring == NULL的话就会达到释放指针的效果
	// 释放旧的字符串
	if (this-&gt;m_cstring != NULL) {
		cout &lt;&lt; &quot;operator=(const char* cstring) - delete[] String: &quot; &lt;&lt; this-&gt;m_cstring &lt;&lt; endl;
		delete[] this-&gt;m_cstring;
		this-&gt;m_cstring = NULL;
	}
	if (cstring != NULL) {
		// 创建字符串空间
		this-&gt;m_cstring = new char[strlen(cstring) + 1]{};
		// copy赋值
		strcpy(this-&gt;m_cstring, cstring);

		cout &lt;&lt; &quot;operator=(const char* cstring) - new - String: &quot; &lt;&lt; cstring &lt;&lt; endl;
	}

	return *this;
}

ostream&amp; operator&lt;&lt; (ostream &amp; cout, const String &amp; string) {
	if (string.m_cstring == NULL) { return cout; }
	return cout &lt;&lt; string.m_cstring;
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[学习优秀的iOS代码(SDWebImage)]]></title>
        <id>https://glve1027.github.io/post/xue-xi-you-xiu-de-ios-dai-ma-yi/</id>
        <link href="https://glve1027.github.io/post/xue-xi-you-xiu-de-ios-dai-ma-yi/">
        </link>
        <updated>2020-01-22T13:31:58.000Z</updated>
        <summary type="html"><![CDATA[<p><a href="https://github.com/SDWebImage/SDWebImage">SDWebImage</a>,一个iOS图片加载库，在iOS界，一直是标杆的存在，本篇博客不去讨论他的具体代码架构、设计、算法，而是从一个个小的tips来发现这些代码的闪光点。</p>
]]></summary>
        <content type="html"><![CDATA[<p><a href="https://github.com/SDWebImage/SDWebImage">SDWebImage</a>,一个iOS图片加载库，在iOS界，一直是标杆的存在，本篇博客不去讨论他的具体代码架构、设计、算法，而是从一个个小的tips来发现这些代码的闪光点。</p>
<!-- more -->
<h3 id="1-协议接口的使用">1. 协议(接口)的使用</h3>
<ul>
<li>自吹一下，作者的这一点写代码的风格和我还是很像的，都很喜欢<code>面向接口编程</code>，整个<code>SDWebImage</code>中存在很多这样的情况，比如：<code>SDImageCodersManager</code>， 光从这个名字就能看出这是一个关于给图片编码的类，再看定义：</li>
</ul>
<pre><code class="language-objectivec">@interface SDImageCodersManager : NSObject &lt;SDImageCoder&gt;
@end

@protocol SDImageCoder &lt;NSObject&gt;
@end
</code></pre>
<ul>
<li>这样代码风格遍布在整个项目中, 例如下面类中定义的<code>SDWebImageDownloaderRequestModifier</code>,<code>SDWebImageDownloaderResponseModifier</code>,<code>SDWebImageDownloaderDecryptor</code></li>
</ul>
<pre><code class="language-objectivec">@property (nonatomic, strong, nullable) id&lt;SDWebImageDownloaderRequestModifier&gt; requestModifier;

@property (nonatomic, strong, nullable) id&lt;SDWebImageDownloaderResponseModifier&gt; responseModifier;

@property (nonatomic, strong, nullable) id&lt;SDWebImageDownloaderDecryptor&gt; decryptor;
</code></pre>
<blockquote>
<p>总结：如果后面我们也想实现一个功能可以模仿这种规范：1：定义一个接口  2：定义一个默认的类并且遵循相应的接口  3：暴露给外面的是一个id&lt;协议&gt;object。</p>
</blockquote>
<h3 id="2-宏的使用">2. 宏的使用</h3>
<ul>
<li>其实如果你正常看第三个优秀源码，你就会发现，很多代码存在很多优秀的宏定义，一方面使用起来很优雅，一方面也可以学习到关于编译器的一个参数的设置。在项目中，我经常看到下面的代码：</li>
</ul>
<pre><code class="language-objectivec">SD_LOCK(self.HTTPHeadersLock);
[self.HTTPHeaders setValue:value forKey:field];
SD_UNLOCK(self.HTTPHeadersLock);

// 定义
#ifndef SD_LOCK
#define SD_LOCK(lock) dispatch_semaphore_wait(lock, DISPATCH_TIME_FOREVER);
#endif

#ifndef SD_UNLOCK
#define SD_UNLOCK(lock) dispatch_semaphore_signal(lock);
#endif

// 用来区分平台的宏
#if TARGET_OS_IOS || TARGET_OS_TV
    #define SD_UIKIT 1
#else
    #define SD_UIKIT 0
#endif

#if TARGET_OS_IOS
    #define SD_IOS 1
#else
    #define SD_IOS 0
#endif
</code></pre>
<blockquote>
<p>总结：没什么技术含量，大家都会这样去实现<code>锁</code>,但是代码未必有这么优雅，点赞。<br>
说道<code>锁</code>的实现，代码中还存在另一个保证线程安全的就是<code>@synchronized(self)</code>, 这里在项目源码中，有时候作者用的是<code>@synchronized(self)</code>, 有时候用的又是一个临时变量：<code>@synchronized(xx)</code>, 网上之前也有一些关于这样<code>性能不是很好的传闻</code>。关于这个我特意去查阅了一些资料，大家可以看一下<a href="http://mrpeak.cn/blog/synchronized/">FaceBook大神的这篇文章</a></p>
</blockquote>
<h3 id="3-关于函数定义规范的问题">3. 关于函数定义规范的问题</h3>
<ul>
<li>在项目中关于下载图片的<code>api</code>，可以说是核心的代码，在看看内部函数定义，代码如下：</li>
</ul>
<pre><code class="language-objectivec">// 参数类型的定义
typedef void(^SDWebImageNoParamsBlock)(void);
typedef NSString * SDWebImageContextOption NS_EXTENSIBLE_STRING_ENUM;
typedef NSDictionary&lt;SDWebImageContextOption, id&gt; SDWebImageContext;
typedef NSMutableDictionary&lt;SDWebImageContextOption, id&gt; SDWebImageMutableContext;


- (void)sd_setImageWithURL:(nullable NSURL *)url {
    [self sd_setImageWithURL:url placeholderImage:nil options:0 progress:nil completed:nil];
}

- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder {
    [self sd_setImageWithURL:url placeholderImage:placeholder options:0 progress:nil completed:nil];
}

- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options {
    [self sd_setImageWithURL:url placeholderImage:placeholder options:options progress:nil completed:nil];
}

- (void)sd_setImageWithURL:(nullable NSURL *)url placeholderImage:(nullable UIImage *)placeholder options:(SDWebImageOptions)options context:(nullable SDWebImageContext *)context
</code></pre>
<ul>
<li>后期定义函数也可以这样，最后收拢到一个核心的函数上。</li>
<li>关于参数的类型，是否需要提前定义，这个从规整的角度来看，还是需要的。</li>
<li>关于函数类型是否为<code>nil</code>，还是需要<code>nullable</code>来标识。</li>
</ul>
<blockquote>
<p>越是规范的东西，越是在写代码的时候可以的去实现，时间长了，也可以学习到优秀代码的规范。</p>
</blockquote>
<h3 id="4-关于网络请求的实现">4. 关于网络请求的实现</h3>
<ul>
<li>这里用自定义的<code>NSOperation</code>, 这里要说一下，自定义<code>NSOperation</code>有两种实现，一种是<code>从写start方法</code>【需要自己去管理isFinish/isExecuting】, 领一种是<code>从写main方法</code>【不需要管理状态值】</li>
<li>并且这里上来就用了<code>@synchronized</code>保证线程安全。</li>
<li>并且这里使用了一个自定义的串行队列来管理<code>_coderQueue = dispatch_queue_create(&quot;com.hackemist.SDWebImageDownloaderOperationCoderQueue&quot;, DISPATCH_QUEUE_SERIAL);</code></li>
</ul>
<pre><code class="language-objectivec">- (void)start {
    @synchronized (self) {
        ...
    }
}
</code></pre>
<h3 id="5-关于代码设计开闭原则">5. 关于代码设计：<code>开闭原则</code></h3>
<ul>
<li>其实在设计<code>Framework</code>的时候，需要时刻记着，哪些东西不应该暴露给用户，哪些需要暴露给用户修改的，这里作者大量使用枚举来实现外部自定义配置：</li>
</ul>
<pre><code class="language-objectivec">if (self.options &amp; SDWebImageDownloaderHighPriority) {
    self.dataTask.priority = NSURLSessionTaskPriorityHigh;
} else if (self.options &amp; SDWebImageDownloaderLowPriority) {
    self.dataTask.priority = NSURLSessionTaskPriorityLow;
}      
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[DFS&BFS 解题心得]]></title>
        <id>https://glve1027.github.io/post/dfsandbfs-jie-ti-xin-de/</id>
        <link href="https://glve1027.github.io/post/dfsandbfs-jie-ti-xin-de/">
        </link>
        <updated>2019-12-26T02:12:29.000Z</updated>
        <summary type="html"><![CDATA[<blockquote>
<p>最近在一直在刷LeetCode, 一直在练习DFS与BFS的题，练多了发现有很多都是有固定套路的心得：</p>
</blockquote>
]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>最近在一直在刷LeetCode, 一直在练习DFS与BFS的题，练多了发现有很多都是有固定套路的心得：</p>
</blockquote>
  <!-- more -->
<ul>
<li>先写一段伪代码，这段伪代码是下面套路的“白话文”</li>
</ul>
<pre><code class="language-swift">// 1. 这里的value 是初始值
func start(value) {
    // 2. 初始值的边界问题
    if (value 不合法) { return }

    // 3. !!关键，这里一定要新建一个函数，用于递归, 见下面的函数bfs
    // 同时，bfs函数的入参，你可以自己不断的加
    // 参数的理解， 如果bfs函数的终止条件 需要通过value 来计算出来，这里就有必要传入进去
    let terminalValue = func handle(value)
    // 参数1：
    bfs(terminalValue, [value])
    // 或者dfs
    dfs(terminalValue, value, [])
}

private func bfs(terminalValue, [value]) {
    // 4. 判断终止条件
    if (terminalValue) { return }
    // 5. 这里需要一个临时的集合，用数组Array
    var tempArray = []

    for v in [value] {
        // 6. !!这里通过v来找到下一个需要遍历的量，这里既可以实现DFS, 也可以实现BFS
        let nextV = func findNextV(v)
        
        tempArray.append(nextV)
    }

    // 递归调用
    bfs(terminalValue, tempArray)
}


private func dfs(terminalValue, value, arr) {
    // 4. 判断终止条件
    if (terminalValue) { return }
    
    // 5. 这里是关键！！判断arr数组逻辑关系
    
    var arrLeftDatas = arr
    if let leftNode = value.left {
        arrLeftDatas.append(leftNode)
        
        dfs(terminalValue, value, arrLeftDatas)
    }
    
    var arrRightDatas = arr
    if let rightNode = value.right {
        arrRightDatas.append(rightNode)
        
        dfs(terminalValue, value, arrRightDatas)
    }
}
</code></pre>
<ul>
<li>
<p>下面看两道题目：</p>
</li>
<li>
<p>LeetCode 199题目</p>
</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://glve1027.github.io//post-images/1577333311197.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-swift">import Foundation

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     public var val: Int
 *     public var left: TreeNode?
 *     public var right: TreeNode?
 *     public init(_ val: Int) {
 *         self.val = val
 *         self.left = nil
 *         self.right = nil
 *     }
 * }
 */
class Solution {
    func rightSideView(_ root: TreeNode?) -&gt; [Int] {
        guard let root = root else { return [] }

        var datas: [TreeNode] = [root]
        var result: [Int] = [root.val]
        var currentLevel: Int = 1

        printRight(&amp;datas, &amp;result, &amp;currentLevel)
        return result
    }

    private func printRight(_ datas: inout [TreeNode], _ result: inout [Int], _ level: inout Int) {
        guard !datas.isEmpty else { return }

        // 记录层数
        level += 1

        var tempDatas: [TreeNode] = []
        for d in datas {
            // 下一个循环要检查的子数，这里都要记录下来
            if let r = d.right {
                tempDatas.append(r)
                if result.count &lt; level { result.append(r.val) }
            }
            if let l = d.left {
                tempDatas.append(l)
                if result.count &lt; level { result.append(l.val) }
            }
        }

        printRight(&amp;tempDatas, &amp;result, &amp;level)
    }
}
</code></pre>
<ul>
<li>LeetCode 994</li>
</ul>
<figure data-type="image" tabindex="2"><img src="https://glve1027.github.io//post-images/1577333441345.jpg" alt="" loading="lazy"></figure>
<pre><code class="language-swift">
import Foundation

class Solution {
    func orangesRotting(_ grid: [[Int]]) -&gt; Int {
        guard !grid.isEmpty else { return 0 }

        var bads: [[Int]] = []
        var visited: [[Int]] = []
        var grid = grid
        var total: Int = 0
        var maxY: Int = 0
        let maxX: Int = grid.count - 1

        for i in 0..&lt;grid.count {
            let temp = grid[i]
            maxY = max(maxY, temp.count - 1)
            for j in 0..&lt;temp.count {
                if (temp[j] == 2) {
                    bads.append([i, j])
                    visited.append([i, j])
                }
                total += 1
            }
        }

        // 起始条件判断
        if (bads.isEmpty) { return hasNoBad(grid) ? -1 : 0  }

        var minu: Int = 0
        countMinute(&amp;minu, maxX, maxY, &amp;grid, &amp;bads, &amp;visited, total)
        return hasNoBad(grid) ?  -1 : minu
    }

    private func countMinute(_ minu: inout Int, _ maxX: Int, _ maxY: Int, _ grid: inout [[Int]], _ bads: inout [[Int]], _ visited: inout [[Int]], _ total: Int) {
        guard !bads.isEmpty else { return }
        guard visited.count &lt; total else { return }

        // 临时收集坏的
        var visited = visited
        var tempBads: [[Int]] = []
        // 遍历
        for b in bads {
            let currentX = b[0]
            let currentY = b[1]
            let currentIsBad = grid[currentX][currentY] == 2
            // 四个方向进行判断
            
            // 1. 上
            let upY = currentY - 1
            if (upY &gt;= 0 &amp;&amp; upY &lt;= maxY &amp;&amp; !containsInDatas(visited, [currentX, upY])) {
                visited.append([currentX, upY])
                if (currentIsBad &amp;&amp; grid[currentX][upY] == 1) {
                    // 已经腐烂
                    grid[currentX][upY] = 2
                    tempBads.append([currentX, upY])
                }
            }

            // 2. 左
            let leftX = currentX - 1
            if (leftX &gt;= 0 &amp;&amp; leftX &lt;= maxX &amp;&amp; !containsInDatas(visited, [leftX, currentY])) {
                visited.append([leftX, currentY])
                if (currentIsBad &amp;&amp; grid[leftX][currentY] == 1) {
                    // 已经腐烂
                    grid[leftX][currentY] = 2
                    tempBads.append([leftX, currentY])
                }
            }

            // 3. 下
            let downY = currentY + 1
            if (downY &gt;= 0 &amp;&amp; downY &lt;= maxY &amp;&amp; !containsInDatas(visited, [currentX, downY])) {
                visited.append([currentX, downY])
                if (currentIsBad &amp;&amp; grid[currentX][downY] == 1) {
                    // 已经腐烂
                    grid[currentX][downY] = 2
                    tempBads.append([currentX, downY])
                }
            }

            // 4. 右
            let rightX = currentX + 1
            if (rightX &gt;= 0 &amp;&amp; rightX &lt;= maxX &amp;&amp; !containsInDatas(visited, [rightX, currentY])) {
                visited.append([rightX, currentY])
                if (currentIsBad &amp;&amp; grid[rightX][currentY] == 1) {
                    // 已经腐烂
                    grid[rightX][currentY] = 2
                    tempBads.append([rightX, currentY])
                }
            }
        }

        // 如果已经没有坏橘子，就提前退出
        guard noBads(tempBads, grid) == false else { return }
        // 累计时间
        minu += 1
        countMinute(&amp;minu, maxX, maxY, &amp;grid, &amp;tempBads, &amp;visited, total)
    }

    private func hasNoBad(_ grid: [[Int]]) -&gt; Bool {
        guard !grid.isEmpty else { return false }
        for i in 0..&lt;grid.count {
            let temp = grid[i]
            for j in 0..&lt;temp.count {
                if (temp[j] == 1) { return true }
            }
        }
        return false
    }

    private func noBads(_ bads: [[Int]], _ grid: [[Int]]) -&gt; Bool {
        guard !bads.isEmpty else { return true }
        for b in bads {
            let x = b[0]
            let y = b[1]
            if (grid[x][y] == 2) { return false }
        }
        return true
    }

    private func containsInDatas(_ datas: [[Int]], _ e: [Int]) -&gt; Bool {
        guard !datas.isEmpty else { return false }
        for d in datas where e == d {
            return true
        }
        return false
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[iOS多线程操作]]></title>
        <id>https://glve1027.github.io/post/ios-duo-xian-cheng-cao-zuo/</id>
        <link href="https://glve1027.github.io/post/ios-duo-xian-cheng-cao-zuo/">
        </link>
        <updated>2019-12-18T07:39:14.000Z</updated>
        <summary type="html"><![CDATA[<p>一旦项目中涉及到多线程操作，就会显得异常复杂，尤其是对数据的操作，如何保证高效的“多读单写”呢？</p>
]]></summary>
        <content type="html"><![CDATA[<p>一旦项目中涉及到多线程操作，就会显得异常复杂，尤其是对数据的操作，如何保证高效的“多读单写”呢？</p>
<!-- more -->
<p>方案有很多种：</p>
<ul>
<li>可以使用<code>锁</code>[这里就不具体展开了，锁的实现太多了]，这里重点说一下第二个方案：</li>
<li>利用GCD实现“多读单写”的功能</li>
</ul>
<pre><code class="language-objectivec">// 创建一个全局的自定义并行队列
dispatch_queue_t globalQueue = dispatch_queue_create(&quot;xxx&quot;, DISPATCH_QUEUE_CONCURRENT);
// 对于&quot;写&quot;的操作
// 1. 保证在我写之前所有的操作已经完成。2. 也只有在我完成此次&quot;写&quot;的操作的时候，下面的操作才能继续执行。
// 在AFNetworking中源码中，用于保护HTTPRequestHeaders的时候使用的是dispatch_barrier_sync(globalQueue, ^{ }); 
dispatch_barrier_async(globalQueue, ^{ });  
// 对于&quot;读&quot;的操作, 同步的获取值
dispatch_sync(globalQueue, ^{ });
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[App主题换肤功能]]></title>
        <id>https://glve1027.github.io/post/app-zhu-ti-huan-fu-gong-neng/</id>
        <link href="https://glve1027.github.io/post/app-zhu-ti-huan-fu-gong-neng/">
        </link>
        <updated>2019-12-17T16:19:47.000Z</updated>
        <summary type="html"><![CDATA[<p>现在国内很多App，每到节假日，都会搞一些活动，这样就免不了需要一个统一管理所谓“皮肤”功能【这里主要指图片、字体、颜色等资源】，下面就是我设计的”皮肤“功能模块。</p>
]]></summary>
        <content type="html"><![CDATA[<p>现在国内很多App，每到节假日，都会搞一些活动，这样就免不了需要一个统一管理所谓“皮肤”功能【这里主要指图片、字体、颜色等资源】，下面就是我设计的”皮肤“功能模块。</p>
<!-- more -->
<h2 id="主题皮肤切换模块">主题皮肤切换模块</h2>
<h3 id="gcttheme配置篇">GCTTheme配置篇</h3>
<ul>
<li>调用<code>GCTThemeManager</code>之前需要注册配置数据, 调用的时机目前是在<code>didFinishLaunchingWithOptions</code>的时候 （这个后期可以修改！！）</li>
</ul>
<pre><code class="language-objectivec">// 1. GCTThemeSwitchObject 控制开关的类，灵活控制开关就可以配置这个类 （面向协议）
GCTThemeSwitchObject *switchObj = [GCTThemeSwitchObject new];
[switchObj setGCTThemeSwitch:YES];
// 2. GCTThemeData 数据源的类，目前只读取本地数据，后期远程数据只需要配置它（面向协议）
[[GCTThemeManager sharedInstanceWithThemeData:[GCTThemeData new]] switchBaseOn:switchObj];
</code></pre>
<h3 id="gcttheme使用篇">GCTTheme使用篇</h3>
<ul>
<li>方法1：如果要修改的控件是<strong>UIView或者是它的子类</strong>并且这个<strong>UIView不等于nil</strong>，可以直接调用方法</li>
</ul>
<pre><code class="language-objectivec">- (void)configSkinWithModule:(GCTThemeModuleTypes)module skinDatas:(NSDictionary&lt;NSString *, NSString *&gt; *)skinDatas;
</code></pre>
<ul>
<li>方法2：如果上面的条件有其中一个不满足，可以直接调用单例<code>GCTThemeManager</code></li>
</ul>
<pre><code class="language-objectivec">// 需要配置皮肤替换功能的，图片获取需要 1. moduleName  2. imgKey
- (UIImage *)getImageByKey:(NSString *)imgKey from:(GCTThemeModuleTypes)module;
- (NSString *)getImageNameByKey:(NSString *)imgKey from:(GCTThemeModuleTypes)module;
// 需要配置皮肤替换功能的，颜色获取需要 1. moduleName  2. colorKey
- (UIColor *)getColorByKey:(NSString *)colorKey from:(GCTThemeModuleTypes)module;
// 需要配置皮肤替换功能的，文字获取需要 1. moduleName  2. titleKey
- (NSString *)getTitleByKey:(NSString *)titleKey from:(GCTThemeModuleTypes)module;
</code></pre>
<h4 id="实际使用情况">实际使用情况</h4>
<ul>
<li>直接配置TabBarController图片、文字。【<strong>因为tabBarItem默认是懒加载的，所以只能通过GCTThemeManager去实现</strong>】</li>
</ul>
<pre><code class="language-objectivec">navController.tabBarItem.title = [[GCTThemeManager sharedInstance] getTitleByKey:model.titleKey from:GCTThemeModuleTypeHomeTabbar];
navController.tabBarItem.image = [UIImage imageMakeWithName:model.imageName type:@&quot;png&quot;];
NSString *imageName = [[GCTThemeManager sharedInstance] getImageNameByKey:model.imageSelectedKey from:GCTThemeModuleTypeHomeTabbar];
navController.tabBarItem.selectedImage = [[UIImage imageMakeWithName:imageName type:@&quot;png&quot;] imageWithRenderingMode:UIImageRenderingModeAlwaysOriginal];
</code></pre>
<ol>
<li><strong>在默认情况下，使用的是背景色</strong>。</li>
<li><strong>在春节皮肤的配置下使用的是图片</strong> 【这里可以以此类推，某种皮肤下面使用的属性1，在另一种皮肤下使用的是属性2，因为当找不到对应的属性值的时候返回nil，也就是不实现】</li>
</ol>
<pre><code class="language-objectivec">self.tipsView.homeBG.image = [[GCTThemeManager sharedInstance] getImageByKey:@&quot;home_wifi_scaning_unconnected&quot; from:GCTThemeModuleTypeHomeWifi];
self.backgroundColor = [[GCTThemeManager sharedInstance] getColorByKey:@&quot;home_wifi_found_scaning&quot; from:GCTThemeModuleTypeHomeWifi];
</code></pre>
<h3 id="gcttheme原理篇">GCTTheme原理篇</h3>
<ul>
<li>GCTThemeSwitchObject遵循的协议</li>
</ul>
<pre><code class="language-objectivec">@protocol GCTThemeSwitchProtocol &lt;NSObject&gt;

- (void)setGCTThemeSwitch:(BOOL)isOpen;
- (BOOL)GCTThemeSwitchIsOpen;

@end
</code></pre>
<ul>
<li>数据源GCTThemeData遵循的协议</li>
</ul>
<pre><code class="language-objectivec">typedef enum : NSUInteger {
    GCTThemeDefaultData,
    GCTThemeFestivalData
} GCTThemeDataTypes;

NS_ASSUME_NONNULL_BEGIN

@protocol GCTThemeDataProtocol &lt;NSObject&gt;

// Getter Method
- (NSDictionary *)skinDataFor:(GCTThemeDataTypes)themeType;

// Setter Method
- (void)setSkinData:(NSDictionary *)data for:(GCTThemeDataTypes)themeType;

@end
</code></pre>
<ul>
<li><strong>GCTThemeManager核心代码</strong></li>
</ul>
<pre><code class="language-objectivec">- (NSString *)findResourceBy:(NSString *)key from:(GCTThemeModuleTypes)module with:(NSString *)subKey {
    // 切换数据
    NSDictionary *resourcesData = self.switchIsOpen ? self.tempSkinData : self.skinData;
    NSString *moduleString = [self convertThemeType2String:module];
    if (moduleString == nil || [moduleString isEqualToString:@&quot;&quot;] || resourcesData == nil || ![resourcesData.allKeys containsObject:moduleString]) return nil;
    NSDictionary *moduleDic = resourcesData[moduleString];
    if (![moduleDic.allKeys containsObject:subKey]) { return nil; }
    NSDictionary *Dic = moduleDic[subKey];
    if (![Dic.allKeys containsObject:key]) { return nil; }
    return Dic[key];
}
</code></pre>
<h3 id="测试篇">测试篇</h3>
<ul>
<li>配置以及Mock数据</li>
</ul>
<pre><code class="language-objectivec">- (void)setUp {
    self.themeData = [GCTThemeData new];
    // Mock Data
    [self.themeData setSkinData:@{@&quot;home_tabbar&quot;: @{@&quot;images&quot;: @{@&quot;image_home_button_selected&quot;: @&quot;Home_Selected&quot;,
                                                                 @&quot;image_video_button_selected&quot;: @&quot;Provider_Selected&quot;,
                                                                 @&quot;image_discovery_button_selected&quot;: @&quot;Discovery_Selected&quot;,
                                                                 @&quot;image_mine_button_selected&quot;: @&quot;Mine_Selected&quot;}}} for:GCTThemeDefaultData];
    [self.themeData setSkinData:@{@&quot;home_middle&quot;: @{@&quot;colors&quot;: @{@&quot;color_before_background&quot;: @&quot;#cccccc&quot;},
                                                    @&quot;values&quot;: @{@&quot;value_home_title&quot;: @&quot;测试&quot;}
                                                    }} for:GCTThemeFestivalData];
    
    
    // Mock Switch
    self.switchObject = [GCTThemeSwitchObject new];
    [self.switchObject setGCTThemeSwitch:NO];
    
    self.manager = [GCTThemeManager sharedInstanceWithThemeData:self.themeData];
    [self.manager switchBaseOn:self.switchObject];
}

/*
 ** 测试是否为单例
 */
- (void)testGCTThemeManagerIsSingle {
    NSMutableArray *managers = [NSMutableArray array];
    
    for (int i = 0; i &lt; 3; i++) {
        dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
            GCTThemeManager *tempManager = [[GCTThemeManager alloc] init];
            [managers addObject:tempManager];
        });
    }
   
    [managers enumerateObjectsUsingBlock:^(GCTThemeManager *obj, NSUInteger idx, BOOL * _Nonnull stop) {
        XCTAssertEqual(self.manager, obj, @&quot;GCTThemeManager is not single&quot;);
    }];
}

/*
 ** 测试开关
 */
- (void)testGCTThemeSwitch {
    XCTAssertFalse(self.manager.switchIsOpen, &quot;default switch is closed&quot;);
    
    [self.switchObject setGCTThemeSwitch:YES];
    [self.manager switchBaseOn:self.switchObject];
    XCTAssertTrue(self.manager.switchIsOpen, &quot;switch is open&quot;);
}

/*
 ** 初始化后数据是否正确
 */
- (void)testGCTThemeInitNoMockDataIsNotNil {
    XCTAssertNotNil(self.manager.skinData, &quot;default data is not to be nil&quot;);
    XCTAssertTrue(self.manager.skinData.count != 0, &quot;default data is not to be nil&quot;);
    XCTAssertNotNil(self.manager.tempSkinData, &quot;temp data is not to be nil&quot;);
    XCTAssertTrue(self.manager.tempSkinData.count != 0, &quot;temp data is not be nil&quot;);
}

/*
 ** 通过key来获取图片
 */
- (void)testGCTThemeGetImageByKey {
    XCTAssertNil([self.manager getImageByKey:@&quot;&quot; from:GCTThemeModuleTypeHomeTabbar], &quot;key is nil image should be nil&quot;);
    XCTAssertNil([self.manager getImageByKey:@&quot;image_home_button_selected_error&quot; from:GCTThemeModuleTypeHomeTabbar], &quot;key error image should be nil&quot;);
    XCTAssertNil([self.manager getImageByKey:@&quot;image_home_button_selected&quot; from:GCTThemeModuleTypeHomeWifi], @&quot;type is error image should be nil&quot;);
    XCTAssertNotNil([self.manager getImageByKey:@&quot;image_home_button_selected&quot; from:GCTThemeModuleTypeHomeTabbar], &quot;key and type is right and data has value, image should not be nil&quot;);
}

/*
 ** 通过key来获取图片名字
 */
- (void)testGCTThemeGetImageNameByKey {
    XCTAssertEqual([self.manager getImageNameByKey:@&quot;image_home_button_selected&quot; from:GCTThemeModuleTypeHomeTabbar], @&quot;Home_Selected&quot;, &quot;image name should be equal&quot;);
}

/*
 ** 通过key来获取颜色
 */
- (void)testGetColorByKey {
    // 从春节主题中获取
    [self.switchObject setGCTThemeSwitch:YES];
    [self.manager switchBaseOn:self.switchObject];
    XCTAssertTrue([[self.manager getColorByKey:@&quot;color_before_background&quot; from:GCTThemeModuleTypeHomeMiddleContent] isEqual:[UIColor colorWithHexString:@&quot;#cccccc&quot;]], &quot;UIColor should be equal&quot;);
}

- (void)testGCTThemeGetTitleByKey {
    // 从春节主题中获取
    [self.switchObject setGCTThemeSwitch:YES];
    [self.manager switchBaseOn:self.switchObject];
    XCTAssertTrue([[self.manager getTitleByKey:@&quot;value_home_title&quot; from:GCTThemeModuleTypeHomeMiddleContent] isEqual:@&quot;测试&quot;], &quot;Title should be equal&quot;);
}
</code></pre>
<h3 id="思考篇">思考篇</h3>
<ul>
<li>配置的时机，需要切换，尽量不要放到<code>AppDelegate</code>中，影响启动时间。</li>
<li><code>GCTThemeManager</code>中的开关<code>GCTThemeSwitchObject</code>以及数据源<code>GCTThemeData</code>相应的属性都是同时又<code>Getter/Setter</code>方法的，<strong>数据不安全！！</strong></li>
<li><strong>！！都要通过字符串去查找，编码不是很友好！（后期会通过R.Obj去修正这个问题。）</strong></li>
<li>统一修改的时机每次启动只有一次，后期这里需要修改。</li>
</ul>
]]></content>
    </entry>
</feed>